# CHANGELOG — 规则典故档案

本文件收纳 SKILL.md 正文剔除的事故实证典故，格式为「规则 → 典故来源」，按主题分组。
规则本体以 SKILL.md / references/*.md 为准，此处只保留来历备查。

## 反幻觉与权限

- 执行前必读防幻觉声明（禁止声称"没有文件系统权限"） → 国电南瑞实证：用户消息带本地路径链接时，模型曾错误声称"没有文件系统权限"并用训练记忆编造分析。

## fill JSON 写盘纪律

- fill JSON 一律 Write 工具直接写盘，禁止 python/bash/heredoc 拼接 → 恒瑞/中免/阿里"卡住"与赢合/东方财富 Python 报错均源于此（Windows 下 GBK 编码/引号转义/大文本截断）。
- 同上规则的违规实证 → 万华 2026-08-26：仍有模型用 `cat >> << 'EOF'` 追加分段，侥幸未炸但属违规（无转义校验、无断点，出问题无法定位）。
- fill JSON 超 ~8k token 必须分 2-3 段写盘 → 紫金实证：16.5k token 单次必挂（撞网关 504/连接中断且重试无断点），6.6k 分段一次过。
- 分段 append 段内必须是合法 JSON 片段 → 腾讯 2026-08-23 实证：append 写成 Python 单引号字典致 `json.load` 失败。
- 自检不走 `python -c` 内联中文 → 中兴 2026-08-24 实证：踩 Windows 控制台引号/编码坑。
- fill 与 E1 落盘在渲染成功后由 render 自动归档 `_archive/`（不再删除）→ v4.9.2 追记调查：
  报告目录堆积 16 个 `_em_*.json` 系清理清单只删 fill 所致；叠加用户数据库需求，v4.10 立
  B-min 归档制（`_archive/fill_{公司名}_{代码}_{日期}.json` + `_index.jsonl` 索引），
  删除流程废止——提前移动/删除仍 = 防伪链断裂拒渲染。

## 渲染纪律

- 不用 Edit 逐段写 HTML，走 fill→render → 国电南瑞实证：18 次 Edit 消耗 2.2M input token，占全程 73%。
- 渲染报错后禁止手写全文 HTML 绕行（修 fill 重渲是唯一合法路径） → 巨石 2026-08-23 绕行实证。
- class 单引号归一由渲染器自动完成，fill 不必手工归一 → 万华 2026-08-26 实证：258 处单引号（v4.4 起自动归一）。
- thesis 一句话结论 = 论点+证据+三情景价+操作结论，不能只塞目标价 → 中兴 2026-08-24 实证（v4.8 起信息量地板告警）。
- `target_range` 只填基础情景区间，填三情景全区间会被计算值覆盖 → 万华 2026-08-26 误填实证。
- 时机判定小表单元格只写短语，长句塞格会横向溢出 → 腾讯 2026-08-23 实证（>40 字告警）。
- 核心结论正文禁止混入评分卡/指标行 → 中芯国际两次实证：conclusion 混入 `.metric-row`。
- 目标价展示精度由脚本统一（<10 元两位小数、其余整数），手写处按真实精度写 → 工商银行 601398
  2026-09-09 实证：7 元价位的真实区间 5.9-6.5/7.5-8.0/8.7-9.3 被 `: .0f` 整数格式化压成
  6-6/8-8/9-9，且 Hero 区间卡/情景表/走廊图/thesis 四处四种精度（v4.10 `_fmt_px` 统一）。
- 三情景表 `trigger` 写 ≤15 字短语、随列右对齐 → 同工行实证：触发条件行长文本被对齐兜底
  剥类成左对齐、与全表右对齐不一致（v4.10 起 scenario-table 豁免对齐自动修正，类名全归脚本）。
- 黄灯扣分明细表只列 points>0 的类别行、表末一句兜底核查 → 同工行实证：a/d 类「无/0」空行
  占用篇幅且无信息；表格为唯一正典（`.deduction` span 降为行内单条引用形态）。
- 损失预演固定三联卡（亏损故事/与清单重合度/清单外新风险）→ 同工行实证：300 字三件事
  糊在单段 danger-card；三联与 scoring.md 强制三步一一对应。

## 脚本使用

- 「不 Read 脚本」细化为「禁学结构、允许 grep 诊断报错」 → 万华 2026-08-26 实证：诊断为定位校验口径读脚本属合理调试。
- em_fetch 输出禁止 `| tail` 截断、委派回传必须含完整 peers 部分 → 新华保险实证（tail 砍头部=丢本体留同业）与神华 601088 委派截断，两次同类翻车立「截断与委派边界」。
- Bash 内联中文调脚本会被 Windows shell 静默吞掉 → 工业富联实证：连续 3 次内联调 E7 全部无输出。
- 东财 secid 探测脚本已内置，禁止手工绕行 → 壁仞项目实证。
- 429 硬停：第一次 429 即停，不得硬挺 → 紫光子代理实证：74 次抓搜索引擎结果页烧穿额度，第一次 429 后又跑 15 分钟。

## 评分与估值

- 估值分只管「价格 vs 价值」，非价格因素归质量分/黄灯 → 腾讯案例：把「AI 变现不确定、回购减半」扣进估值分导致 6.5 偏低，应给约 7.6。
- DCF 触发条件必须显式检查并写出结果，禁止含糊声称用 DCF → 美的实证：5 年均值约 0.6、标准差巨大，两条都不满足，应明确跳过而非声称用 DCF。
- 行业增速口径必须与标的主营匹配 → 中免实证：用「国内出游 +16%」充免税行业增速致 1A 口径虚高。
- 1D 短债覆盖豁免适用对象=高资本开支周期股（宝丰/紫金类），非普适条款。
- PE 校准推导只在第 10 章展开、第 7 章只写结论 → 东方电气实证：同一推导两处重复展开（v4.7.1 起 7/10 分工）。

---

## 版本历史

### V4.10.0（2026-09-11）— 工商银行报告五项 + fill 归档库（B-min）+ 朱雀仿宋字体栈

> 触发：用户评审工商银行（601398）报告提出五项问题，另立项两项（fill 归档库、字体接入）。
> 另有两项经评估**不做**：Tower 并行写 fill（维持 v4.9.1 结论：跨字段一致性门禁只在合并后
> 可验、定性写作禁止下放弱模型、git worktree 隔离对运行时 JSON 产物不对症）；fill 多片段+
> 合并脚本提效改造（留待另行立项）。RENDERER_VERSION "v4.9" → "v4.10"（版本常量同步升档）。

- **低价股目标价精度统一**（工行问题 4，本批唯一数据展示 bug）：情景表「目标价」行、
  Hero 目标价区间卡、走廊图标签原用 `: .0f`/4 位有效数字混排——工行真实区间
  5.9-6.5/7.5-8.0/8.7-9.3 被压成 6-6/8-8/9-9，同份报告四种精度并存。新增
  `charts_base._fmt_px`（<10 元两位小数、≥10 元整数），四处统一复用。
- **三情景表触发条件行右对齐**（问题 5）：触发条件格原发无类 `<td>`，被
  `fix_table_alignment` 长文剥类成左对齐、与全表右对齐不一致。`build_scenario_block`
  表头/时间维度/触发条件全部发 `num` 类 + `fix_table_alignment` 豁免 scenario-table
  （类名脚本写死，多数决不再介入）；fill-schema 补 `trigger` ≤15 字短语写作规则。
- **黄灯扣分明细只列扣分项**（问题 1）：表格定为唯一正典（`.deduction` 降为行内引用形态），
  无扣分类别不入表 + 表末一句「其余类别已核查无扣分」兜底；`_check_l4_order` 扩展识别
  表格形态（原正则只认 `<strong>(a` 行内形态，工行表格曾完全绕过顺序检查）。
- **损失预演三联卡**（问题 2）：新增 `.pm-grid/.pm-card/.pm-head` 卡族（danger 红色家族，
  ≤768px 落单列），三卡=最可能的亏损故事/与红黄灯清单重合度/清单外新风险，与
  scoring.md 强制三步一一对应；fill-schema 固定骨架，禁退回单段长文。
- **行业 fin_trend 推荐面板**（问题 3）：工行「净息差×不良贷款率、拨备覆盖率×核心一级
  资本充足率」两面板系模型临场发挥（文档无规定），本版固化进四个行业附录
  （金融按银行/保险/券商分列，含数据路径与不可得回退）；fill-schema fin_trend 行加指针，
  行业专项指标（不在 E3 管道）放开妙想 F9/年报手查口子并须 .source 注明出处。
- **fill 归档库 B-min**（数据库决议）：渲染成功后 render 自动把 `_fill_*.json` 与 E1 防伪
  落盘移入报告目录 `_archive/`（命名 `fill_{公司名}_{代码}_{日期}.json` /
  `em_{代码}_quote_{日期}.json`），并追加 `_archive/_index.jsonl` 索引行（日期/公司/代码/
  三轨分/现价/目标价区间/红灯/RENDERER_VERSION）——跨报告对比与评分校准的数据源。
  只动 `_fill_`/`_em_` 前缀文件（fixture 不误移）；归档失败降级告警不炸渲染；
  `--check` 不归档。v4.9.2 追记方案 A（删除制）正式废止。磁盘量：fill 约 30-60KB/份，
  一年 200 份 ≈ 10-15MB。peers/AH 额外落盘文件按 SKILL.md 命名规则手动归档。
- **朱雀仿宋 F1 字体栈**：body font-family 栈首加朱雀仿宋（OFL，TrionesType/zhuque，
  本机自装才生效；未装终端 fallback=原栈零差异）；全报告统一仿宋（含表格/SVG，数字列
  tabular-nums 对齐让位，用户已拍板接受）。**坑**：当前发行版注册家族名实为
  「朱雀仿宋（预览测试版）」——栈首发精确名才匹配（Word 可见≠浏览器能匹配，
  用 System.Drawing InstalledFontCollection 查注册名实证）；无前缀名保留兼容未来正式版。
  字体内嵌（F2 子集化）留待 F1 本机效果评估后决定。
- **美图 01357 端到端比对**（v4.9.1 结转，本版完成）：`em_fetch.py 01357` 实跑，
  timing 五值（现价 4.24 / MA60 4.33 / MA120 4.50 / 52 周高低 10.12/3.61）与用户 09-05
  手工纠正值（4.95/4.34/4.53/11.82/3.61）同口径——差异全部由 6 天窗口滑动（52 周高点
  滑出窗口）与期间股价下跌解释；1644 个交易日序列确认排序修复在真实数据上工作。
- **测试**：七套回归全绿（test_render_core 38→42：低价精度/触发行对齐/黄灯表顺序/归档
  四新增；golden 有意重建一次——模板 CSS 三处 + fixture l4 形态，重建后标记核验复跑通过）；
  demo 重渲（黄灯表只列 b/c + 三联卡）+ 无头 Edge 桌面/移动双端截图目检通过。

**同日二次修订（用户用部署版重跑工行 09-11 报告，自查发现六项，全部定位修复）**：

> 根因分两类：①**写作端照抄 09-09 旧报告形态**（黄灯空行/损失预演单段/peers 零标注/
> price_history 的 pe 全缺）——软规则无门禁就没人守，且渲染时间（16:57）在部署之后，
> 排除部署时滞；②**渲染器两个真 bug**（预期差表对齐锯齿、tbody th 斑马纹断裂）。
> 工行 09-11 报告已按新形态机械修补重渲（分析内容不动），旧形态 fill 留存
> `_archive/…_门禁打回旧形态留存.json` 备查。

- **L4 固定形态硬门禁**（`_check_l4_form`，拒渲染级）：l4_html 缺 `.pm-grid` 三联卡 → 拒；
  黄灯扣分明细表行数 ≠ `yellow_deductions` 条数 → 拒（多列=零扣分空行该删、少列=扣分项
  没列全）；类别字母缺失无法核对 → 降级告警。工行归档 fill 复跑 `--check` 实证被该门禁
  打回（退出码 2、理由正确）。
- **peers 最优/最差零标注告警** + **price_history 的 pe 过半缺失告警**（E2 月线自带月末
  PE(TTM) 应照抄，港股不可得请注明）——均为告警级（数据可得性存在合法差异）。
- **fix_table_alignment 平票判左**（预期差表锯齿修复）：原规则平票判 num 导致同列
  「数字右、文字左」zigzag（同一个「—」在不同列一左一右）；平票改判左后文本为主的表
  整表左对齐。写作端无责——fill-schema :69 本就规定该表「单元格不加类」。
- **模板 CSS：tbody th 归一**（工行同业表公司名写 `<th>` 实证）：`th` 米黄底色原无差别
  穿透 tbody、斑马纹/hover/冻结列只认 td——tbody th 归一为 td 同式，斑马/hover/冻结列
  规则全部扩到 th；thead 表头样式不受影响（`.freeze-first thead th:first-child` 保留米黄）。
- **backtest.md 6.6 补纪律**：复盘/再分析所有固定形态段必须按当前 fill-schema 骨架重写，
  禁止照抄旧报告（旧形态会被门禁打回）；fill-schema 表格节补平票判左与 th 归一说明。
- **测试**：test_render_core 42→46（L4 门禁三分支/平票判左三分支/peers 标注告警/
  price_history pe 告警）；test_mcap_mode fixture l4 升级三联卡；golden 有意重建第二次
  （模板 CSS 四处，diff 确认零表体漂移）；七套全绿。
- **遗留观察**：09-11 重跑未进回测模式（无 prev/复盘标记，尽管 09-09 报告存在）——
  或为用户明示全新分析（豁免条款），或为触发遗漏，待用户确认；若是后者，
  extract_review --find 的触达率需另行加固。

### V4.9.2（2026-09-07）— 甘李药业/三环集团报告流程评审五项：fill-schema 超长行拆分 + trig 卡双数取整与贴顶对齐 + 同业表高亮去底

> 触发：用户评审甘李药业（603087）与三环集团（300408）报告生产流程，提出五项问题。
> 定位经会话日志取证 + 无头浏览器像素级复现，两项用户前提被证据纠正（见下）。
> RENDERER_VERSION 常量保持 "v4.9"（沿用 v4.9.1 各批补充修订惯例，版本号只走文档）。

- **fill-schema.md 第 61 行拆行**（问题 1）：被截断的不是甘李 fill JSON，而是 fill-schema.md
  自身第 61 行（`l1_html…l3_html` 字段定义行，2132 字符，全仓库唯一超 Read 工具 2000 字符
  截断线的行）。甘李会话内模型已用 `sed + cut` 补读成功、无副作用；本版根治——该行拆为
  短指针 + 表格下方「l1_html / l3_html 写作细则」小节（内容零增删，仅在地标处插换行，
  拆后最长行 873 字符）。
- **甘李「9 分钟」不属实**（问题 2，仅取证记录、无代码变更）：日志时间戳实证甘李全程
  24.5 分钟（08:28:00→08:52:31）、三环 35.6 分钟；用户所见 9 分钟 ≈ fill 写盘→渲染段。
  与截断无关；必读文件、peers 委派、--check 循环（3 轮）等硬门禁全部执行。
- **trig-strip 双数取整**（问题 3）：`.trig-strip` 恰 2/4 张卡时桌面改 2 列（4=2+2、2=1+1），
  3/5/6 保持 3 列。纯 CSS `:has()` 数量查询实现，包在 `@media (min-width:1025px)` 内防压过
  移动端规则；旧浏览器无 :has 回退现状。受益面：3.3 护城河/3.5 治理/4.2/4.3/13 章 triggers。
- **trig 卡行内贴顶对齐**（问题 4a）：`.trig` 加 `align-content: flex-start`——原默认
  stretch 把短内容卡片的多余空间摊进各行线盒、标题被居中下推（甘李 3.5「股权与控制权」
  文字块较同行动卡低约 20px 实证）；改后同行标题/徽章/论据起点恒对齐。
- **同业表 cell-best/worst 去底改色**（问题 5，用户指定方案）：取消染底（与隔行斑马纹叠加
  显花，甘李 4 行 12 处高亮实证），改纯文字——best 红加粗 #b84a4a（红=强，A 股惯例）、
  worst 黑加粗 #2b2620；选择器由三元素简化为单类。fill-schema.md 写法文档同步。
- **未复现不修**（问题 4b，遗留观察）：3.6 资本回报质量 metric-row「WACC 8% 低于左右」
  未复现——提取甘李报告原 DOM+模板 CSS 无头渲染，三卡标签均单行、数值严格对齐；
  `.metric-card` 为块级流从顶排布，WACC 标签最短，无下沉机制。若再现，预案为
  grid+subgrid 三行共享轨道（需同步 768px 规则）。
- **测试**：七套回归全绿；golden 快照有意重建一次（模板 CSS 三处变更），重建后逐项标记
  核验（`align-content: flex-start` / `:has(> .trig:nth-child(4)` / `td.cell-best` 新色值）
  并复跑通过；demo 重渲 + 甘李真实片段新 CSS 无头截图目检（2+2 分排/贴顶对齐/红粗无底色）
  全过。

### V4.9.1（2026-09-04）— 全面审计修复：残骸清理 + 版本标签归一 + charts 壳删除 + 解析合并 + 文档补齐

- **失败残骸坟场清理**：删除 artifacts/ 全部内容（约 4.1MB：v4.8-v4.9 调试截图、
  重构备份 `_render_report_phase_a_backup.py`/`_em_fetch_pre_split.py`、一次性迁移脚本、
  各版本冒烟 fill、预览 HTML、案例报告——均未追踪、不部署、无引用）与 scripts/__pycache__。
- **版本标签归一**：v4.9 合并发布时遗留的 29 处「v4.10」误标（charts 六模块文件头、
  validate/scoring/render_report/monthly_checkup/测试/fill-schema/模板 CSS 注释）
  全部改回 v4.9；模板注释变更触发 golden 快照有意重建一次。
- **charts.py 兼容壳删除**（42 行）：render_report/validate 改从 charts_base 与图族模块
  直导；render_report 三段 `# noqa F401` 再导出块从 96 名收窄到 7 名
  （= 测试经 R.* 消费且自身未定义的全集），注释如实标注用途。
- **em_fetch re-export 收窄**：40 名 → 21 名（删 5 缓存容器 + 14 私名 helper，
  全仓库 grep 零消费实证；docstring 改为如实描述）；data-sources.md/scoring.md
  两处 `em_fetch.py::私名` 文档前缀改指真身模块 em_data.py。
- **文件名解析合并（owner = extract_review.parse_report_name）**：extract_review 新增
  `parse_report_name()`（三代命名：新命名/带「-复盘-」回测版/pre-v4.0 旧 _ 分隔命名；
  代码 6 位 = A股/北交所、5 位 = 港股；返回 company/code/quality/valuation/date/is_review），
  `find_prev_report` 内部改用它——旧 _ 分隔命名自此也参与回测触发匹配（原 `-{code}-`
  子串判定漏掉 _ 命名的 pre-v4.0 报告，宽松档名存实亡）。score_calibration 删本地
  `parse_filename`、monthly_checkup 删本地 `_REPORT_RE`，均改为 import 复用。
  **monthly_checkup 修复性口径变化**：开始识别回测版（旧正则不认「-复盘-」段，同代码
  最新一份会被漏成旧版）与旧 _ 命名报告；旧命名无估值分显示为「—」。
- **--disclosure 修复**：删掉 `code + (".SH" if startswith("6") else ".SZ")` 手拼
  （港股 5 位/北交所 43/83/92 段静默拼错后缀），改复用 `em_fetch.fetch_disclosure`
  （E1 同款口径：to_ts_code 市场映射含北交所→.BJ；按报告期倒序、优先未披露的最近期）；
  港股 5 位显式跳过（tushare disclosure_date 仅覆盖沪深）。两处「口径待实测」注释随
  复用 E1 已验证口径而落实删除。顺带修复：多只标的同一审查日触发 `overdue.sort()`
  元组比较 dict 的 TypeError（改按日期键排序）。
- 测试：test_extract_review 5→7 项（+parse_report_name 三代命名/边界、+旧 _ 命名触发匹配）；
  test_calibration 第 1 段断言按 dict 口径改写（`无日期-600989-6.0-5.0.html → None` 边界保留）；
  新增 test_monthly_checkup 2 项（合并口径扫描、--disclosure ts_code 映射 mock 禁网络）。
  backtest.md 6.5 增「月度体检」用法小节。
- **helper 去重与死物清理**：`_prev_track_rows` 共享实现落 charts_base
  （validate.build_prev_strip 与 charts_misc.build_review_dumbbell 的旧键回退契约合并）；
  push2his kline URL 收编为 em_data `_em_kline_url`（klt 参数化，score_calibration 删本地实现）；
  散色值收编（`_C_PAPER_CELL`/`_C_BADGE_DEEP` 入 charts_base，三处硬编码改引用，色值逐字符不变）；
  extract_review 删本地 `_num` 改从 scoring 导入；死物清理（charts_base 死 import sys、
  score_calibration 死 import datetime、test_mcap_mode 死变量 SKILL_DIR、
  deploy.ps1 排除清单删已不存在的 .backup-pre-v48）。
- **文档补齐**：SKILL.md 补 v4.9 必填图字段（fin_trend/growth_plot 必填拒渲染、triggers 可选，
  236-241 枚举段 + 自检清单各一处）；README scripts/ 目录树 7→24 项、测试「四个」→「七套」。
- 测试套件由六套增至七套（新增 test_monthly_checkup），全部回归全绿。
- **补充修订（2026-09-05，药明康德 603259 复盘报告 8 项反馈）**：
  ① fin_trend 图墙标准 4 面板改为「营收×毛利率 / 归母净利+扣非×净利率×ROE / 现金流×现金含量 /
  应收+存货周转天数（纯双线）」——E3 年表新增扣非净利列（tushare profit_dedt 主源、东财 F10 兜底）
  与周转天数逐年行；渲染器/validate 支持无柱纯线面板（原 `bars and lines` 双双必填会跳过）。
  ② 取消单柱「最新年钢蓝」高亮——钢蓝此前一身三职（单柱最新年/双柱第二系列/线第一系列），
  柱色统一为只表系列归属（沙=第一系列/钢蓝=第二系列），最新年信息由头行最新值+同比与
  x 轴最右位置承载。
  ③ 3.5 治理与资本配置改用 `.trig-strip` 分块列示（复用 triggers 类族，绿=正面/红=风险/灰=中性）。
  ④ 3.6 资本回报质量块首加 metric-row 三卡（ROIC 近 3 年 / WACC 8% / 覆盖倍数），不做独立 SVG 图
  （ROIC 不在 E3 管道，单开数据链不值）；scoring.md 1F 补分型特例（未盈利/管线与困境反转豁免
  ROIC vs WACC 主判据）。
  ⑤ 同业当前指标表新增 `.cell-best`/`.cell-worst` 淡底类，每行按指标方向性各标一格
  （三元素选择器+晚于斑马纹声明，否则偶数行斑马盖掉高亮）。
  ⑥ `holders` 户数趋势图移至 11 章章首（原夹在手写正文与三轨判定卡之间，打断判定→结论阅读流）。
  ⑦ 回测模式废止手写「旧触发条件核对表」（v4.9 triggers 状态条本就承载核对结果，双写冗余）——
  核对结果写 triggers（target 写「阈值｜实际值」），prev 已填而 triggers 缺失渲染器告警。
  ⑧ 13 章合并单表新增 `.dash-table` 类（指标/当前值/阈值列 nowrap、操作/来源长文列 12px）+
  强制 table-scroll+freeze-first。
  测试：test_render_core 34→36（户数图章首断言、单柱无钢蓝、纯线面板、cell-best 类存活、
  回测 triggers 告警），golden 快照随模板/图墙有意重建并逐项核验；E3 实跑 603259 验证
  扣非列与周转行非空。示例文档：artifacts/ 下 demo fill + 渲染产物（目检用，不入库）。
- **补充修订二（2026-09-05，科大讯飞报告 4 项展示层反馈）**：
  ① 核心结论四段长文改 `.concl-grid` 2×2 **结论卡网格**——四卡固定序（关键优势/关键弱点/
  当前市场认知/核心投资逻辑），每卡=卡头+2-4 句浓缩要点+卡尾 `.concl-ref`「详见 X 章」锚索引
  （优势→#s3／弱点→#s5／市场认知→#s8／投资逻辑→#s7·#s11，只链无条件章节）；
  解决信息量大但重点不落地——浓缩省略的论据由被引章节承载，结论章不重复展开。
  ② 3.5 治理分块收紧为**单行 trig 行**（dot+cond+mt+status 全内联，禁止 trig 后另起 `<p>` 正文，
  整块 `<p>` 恰为 2=判词+评分末拍，超出渲染器告警）；**评分末拍强制依据链**
  （基准 X.X + 加分项 − 扣分项，引用块内数据）——科大讯飞报告「看不到扣分/评分依据」实证。
  ③ 同业当前指标表与 3 年趋势表统一为**公司=行标题**（原指标行/公司列与报告内其他表方向不一）；
  cell-best/worst 随之改按指标列标注，且由「仅染淡底」升级为**染底+同色加粗**（淡底对比度过低实证）；
  目标公司蓝粗标在行首格，落 `<th>` 表头=旧方向，渲染器告警。
  ④ 红灯检查改 **hero 三卡**（5 章章首固定：a 财务造假与极高杠杆 / b 立案调查与重大违规 /
  c 主营不可逆衰退），卡面=红灯名+大字 通过/不通过（pass 绿/fail 红）+小字核查路径或命中事实；
  1 章命中时 danger-card 脚本注入逻辑不变。
  改动全部落在模板 CSS + fill-schema 写作规范 + validate 告警三层，render_report.py 主逻辑零改动。
  测试：test_render_core 36→38（3.5 单行化告警两分支、peers 方向告警两分支；结论卡结构用例改写），
  golden 快照随模板 CSS/ fixture 新形态有意重建并逐项核验；demo 重跑覆盖新形态。
- **补充修订三（2026-09-06，美图 01357 港股报告 5 项反馈）**：
  ① **港股日线排序 bug 修复（数据正确性）**：tushare `hk_daily` 返回倒序（新→旧），
  `_hk_daily_series` 未排序直接进缓存——`fetch_timing_material` 港股分支尾部切片
  （closes[-60:]/[-250:]）吃到最旧数据，MA60/52 周高低全错（美图实证：算在 2020 年数据上，
  用户手工重算纠正为 4.95/4.34/4.53/11.82/3.61）。根治于源头：缓存前统一按 trade_date
  升序（quote 取 max/月线 dict 聚合两消费方免疫复查）。回归：test_em_fetch 新增 9b 段
  （倒序输入 → MA60/52 周窗口精确断言 + 月线消费方免疫）。
  ② **失败哨兵 + timing stderr 告警**：hk_daily 限流实为 **1 次/小时**（2026-09-06 复测，
  此前「1 次/分钟」记录作废，与 score_calibration.py 既有注释一致）——失败不写缓存时
  同进程 E1/E2/timing 三消费方各自重试、三连撞限流（当日实测踩中）。`_hk_daily_series`
  失败写空哨兵，消费方命中即走各自降级；`fetch_timing_material` 裸 except 改 stderr 告警
  （参照 fetch_debt 同款），限流/取数失败不再静默吞成 None。data-sources.md /
  data-collection.md 限流口径同步修正。
  ③ **妙想 skill 替补通道写入降级链**：妙想 MCP（`mcp__mx-ds-mcp-stdio__mx_*`）缺席时
  经 Skill 工具调 `mx-data`（定量）/`mx-search`（定性检索）/`mx-xuangu`（选股）替补
  （同一东财妙想 API，密钥配在 skill 内）——SKILL.md 工具检查硬门禁改三级判定
  （MCP → mx skill → 标注降级），data-sources.md 降级链图与妙想段、data-collection.md
  工具探测/定性通道表/港股 E3 规则同步。
  ④ **3.5 治理 trig 条目名定宽对齐**：`.trig-cond` 加 `flex: 0 0 7em`（≤7 字单行、
  超长自动换行，行间 mt 列对齐——美图「大股东持续减持」7 字实证），`.trig-status`
  加 `flex-shrink: 0`；同类族 13 章 triggers 状态条同步受益。fill-schema 补
  「cond ≤7 字为宜」。
  ⑤ **3.5 重点事实深色加粗**：新增 `.trig-mt strong { color:#2b2620; font-weight:700 }`
  （灰字论据中关键事实跳出）；fill-schema 补「金额/时间窗/比例用 <strong> 包裹，
  每块至多 1 处」。
  另：财务健康图墙缺第 4 面板（应收+存货周转天数）经核实为**设计内行为**——港股 HKF10
  无周转天数字段 + 互联网附录已替换应收/存货口径，不改。
  golden 快照随模板 CSS 有意重建并逐项核验；demo 补 7 字条+strong 实证形态。
- **补充修订四（2026-09-06，云铝股份报告 3 项版式反馈 + 1 项讨论）**：
  ① **trig 类族卡片化（3.5 治理分块 + 13 章 triggers 状态条同源受益）**：`.trig` 单行
  flex 改 `flex-wrap`+`order` 重排（HTML 四 span 结构不变，纯 CSS）——标题 cond 居中独占行
  （允许两行）、dot+status 居中徽章行、论据 mt 独占行左对齐；`.trig-strip` 网格桌面 3 列、
  ≤1024px 两列、≤768px 单列（此前移动端无回退，2 列×4 段挤压，云铝手机端实证）。
  cond 定宽 7em（修订三）随之作废，fill-schema 3.5 条款改写。
  ② **dash-table 移动卡片化**：≤768px 行转卡片（tr 边框圆角底色、td 块级带
  `td::before` 列名标签，六列列序写死 CSS、手写 dash_html 结构不变）；表头手写
  `<tr><th>` 无 thead 包裹（浏览器归入 tbody）——整行隐藏须 `tr:has(th)`；
  卡片内 `td.num` 改左对齐、freeze-first 首列冻结解除、斑马纹清除。
  ③ **结论四卡统一短列表化**：四卡统一 `<ul>` 短列表（每条 ≤12 字短语 + 至多一个全角括注
  （数字/日期/来源 ≤15 字，不加类）——①关键优势恰 3 条壁垒短语（云铝：水电成本壁垒
  不可复制/报表现金含量足/业绩弹性一落地）、②弱点 2-3 条、③市场认知恰 2 条（卖方假设
  带来源日期+定价隐含）、④投资逻辑恰 3 条固定槽位（论点/可观测证伪条件/路径判断——
  证伪条件是回测输入，必须完整）；`.concl-card ul` 去 bullet、加粗深色+钢蓝色点前缀。
  结论章定位由「摘要复述」转为「记忆点+索引」，展开论述由被引章节承载。
  结论内容地板 200→120 字（四卡短列表化后原地板误伤合规卡）。
  ④ **3.3 护城河清单化**：复用 trig 类族（零新 CSS）——cond=护城河名称、mt=一句论据、
  status=评级三态（绿=宽阔/灰=中等/红=狭窄）；`<p>` 恰为 2 规则与 3.5 全同。
  ⑤ **4.3 催化剂清单化**：同款 trig 清单（事件+时间窗+影响，已发生/进行中/已证伪）——
  与 13 章触发条件前后呼应（4.3 是预判、13 是跟踪核对）。
  ⑥ **4.2 项目确定性可选清单**（项目名+产能/投产时间+进度三态）；单项目或进度交织的
  标的仍可整段论证——可选形态不强制。
  ④⑤⑥ 校验：`_check_governance_strip` 泛化覆盖 3.3/4.3（trig-strip 块 `<p>` >2 告警），
  test_render_core 补 3.3/4.3 用例。
  ⑦ RIM 估值（剩余收益模型）经讨论**不立第三估值方法**——四件套校验链全围绕 PE 口径，
  平行 BVPS/ROE 口径会翻倍校验复杂度且不消除周期股常态 ROE 的主观假设；其关系式
  PB≈(ROE−g)/(r−g) 后续可作「合理带推导器」进 scoring.md（复用 metric_label 口径替换口），
  本轮不动。
  测试：50 项全绿（test_render_core 增补 3.3/4.3 `<p>` 校验用例），golden 快照随模板 CSS
  有意重建；demo 补四卡 ul 短列表、3.3 护城河/4.2 项目/4.3 催化剂清单形态，桌面（1720px）
  + 窄屏（502px）双宽度目检通过（含 dash 移动卡片、①-④卡括注锚）。

### V4.9（2026-09-04）— 青啤复盘七项改进 + 历史欠账清理（配色翻转 + 双新图 + DCF 分型双轨 + 挂起项 28 项）

青岛啤酒报告复盘（用户反馈七点，讨论定案）落地。**渲染行为变更（golden 快照已重建）**：

- **配色语义拆分与翻转**：方向色 `.up/.down` 翻转为**红涨绿跌**（A 股惯例；此前为国际惯例
  绿涨红跌）；新增评价色 `.good/.bad`（绿好红坏不变）——三指标卡赔率/离散度、回测对比条
  分数差从 up/down 改挂 good/bad；龙卷风/徽章/散点象限等评价语义红绿不动。
  小字灰 `#8a8375` 压深至 `#6f695e`（对比度达标），`danger-card` 背景暖化（`#f9eeea`），
  业务构成图短条占比文字改深墨色（原沙色对比度不足）。
  **taste-skill 整体审计补充**：钢蓝 `#4a6fa5` 与暖象牙纸的搭配成立（唯一冷色强调、
  饱和度 38%、对比度约 4.7:1，保留）；但 pastel 语义色承载白字/小字不达标——
  文字与徽章统一改用加深系（红 `#b84a4a` / 绿 `#4d804d` / 橙 `#96691a` / 灰 `#7a715e`，
  全部 ≥4.5:1），覆盖徽章/涨跌/评价/情景/红旗/估值分大数字/黄灯扣分文字；
  SVG 大面积填充（龙卷风/走廊/象限底色）保留 pastel 原色。
- **新图·财务健康五年组合图墙**（`fin_trend` 必填字段，锚点 `<!--FIN_TREND-->` 挂 3.4）：
  **两列双轴组合图面板**（柱=金额·左轴亿、线=比率·右轴%/倍，柱不断轴、双线第二条灰虚线、
  红虚线=阈值、文字带光晕防柱线叠字），标准 4 面板：营收×毛利率 / 归母净利×净利率×ROE /
  经营现金流×现金含量（阈值0.7) / 货币资金×负债率；**替代 3.4 手写 5 年年表**（schema 禁写）；
  有效面板 <3 或年数 <3 拒渲染（二轮按用户反馈由 8 格单指标小图改为合并面板，
  表现形式参照 lieflat-charts 组合图语法）。
- **新图·利润增长图**（`growth_plot` 必填字段，未盈利/管线分型豁免，锚点 `<!--GROWTH-->`
  挂 4.1）：历史收入/净利增速并列柱 + 预测段本文区间竖条 + 卖方一致预期◆ +
  预期差标注（本文中枢−一致预期），全图中性色不涉涨跌红绿；高度 340→288（用户反馈压高）。
- **产业链图纵向均衡**：两栏各自在总高内均分间距（短栏拉开、上下对齐），
  修青啤 4 上游 vs 5 下游的偏沉观感。
- **业务构成标题怪胎修复**：`segments.period` 限 ≤8 字（超长/含口径字样告警），
  新增 `segments.note` 承载口径清洗说明（渲染为图注小字）；schema 禁止正文重复图标题引言句
  （青啤 70 字 period + 双重标题实证）。
- **关键变量命名规范**（fill-schema p0_html/sensitivity）：变量名单概念+黑话白话括注+
  正文不重复「（第一变量）」图标签（青啤「①吨价/产品结构（第一变量）」难读实证）。
- **DCF 分型双轨**（scoring.md「DCF」节重写）：稳健成长/稳定价值（除银行保险）→
  **强制三行 sanity check**（保守 DCF 值 / 现价隐含永续增速 `g = WACC − FCF₁÷EV` /
  base rate 对照判词，定位下界参考不进估值分权重）；周期/快成长/未盈利/困境反转维持
  FCF 门槛条件触发。SKILL.md 分型表两行同步。
- 测试：test_render_core +4（链图均衡/图墙/增长图/评价色断言）；
  minimal_fill 与 mcap 夹具补齐新必填字段；mcap「归母净利」断言收窄到情景表本体。

**同轮历史欠账清理**（v4.8–v4.8.3 handoff + 全面审计报告 + v4.7.1 挂起清单通读，28 项按
「旧项先对账、后加内容优先」纪律执行；对账改写/跳过理由见 handoffs/v4.9-handoff.md 对账表）：

- **批次 A 纯文档**：Phase 1 三路并行成文；委派规则改自洽三层骨架（P2-K，与「E1 主会话自跑」
  条款对账）；三态标注/妙想缺席/港股现金含量跨文件重复指针化（P1-G）；16 条事故典故从
  references 迁本文件主题段（教学锚例保留）（P2-I）；Step 1.0 路径占位符；backtest.md 6.6 增
  「规律反例回写（MANDATORY）」；data-sources.md 增限流预算节。
  **对账跳过**：「分型规则以 fill-schema 为权威」系 v4.7.1 旧注，现状权威在 SKILL.md
  Phase 0.5，未做删除性操作。
- **批次 B em_fetch 效率+拆分**：`_em_f10` 四调用点合一（`_EM_F10_CACHE` pageSize=20 一次拉+
  `_f10_is_annual` 本地过滤——原案 size=12 不满足 5 年报兜底语义，对账修正）；daily 单日窗口
  并入 430 天序列（quote→timing 请求 2→1）；`_TS_CACHE` per-key 锁（8 线程同冷键 1 请求实测）；
  `_ts_annual_rows` 84 行拆四函数；datacenter 五函数抽 `_em_dc`；`EM_FETCH_DEBUG=1` 末尾统计行；
  E1 时机素材对账（现状已具备，只补「月收序列见 E2」指引，不重复造口径）；E1 并入线程池
  （耦合评估后可行）；**em_fetch.py 1991→961 行 + em_data.py 1064 行 + em_cache.py 62 行**
  三模块拆分——传输/缓存状态留宿主命名空间（test 的 rebind 契约锁死边界），取数函数族经
  转发 stub 承接 rebind，test_em_fetch 一字未动 11 段全绿。
  **对账跳过**：价格缩放不集中（东财子端点×市场双重口径差异，硬抽象掩盖差异）。
- **批次 C 渲染器/校验/结构**：Hero 文案分位/历史带口径告警（含极性核对）；conclusion 四段
  缺段/乱序告警；peers_plot ROE 量级倒挂告警（无外部比对源的次优路径）；复盘「未命中无
  规律反例」弱告警（方向二代码侧）；pe/mcap 双口径解析抽 `_scenario_numbers` 共用；
  `build_position_card` 拆 `_position_steps` 纯函数 + 7 分支单测；extract_review 大文件
  头尾读取优化；**`{{KEY}}` 字面量被模板二轮替换误吞的真 bug 修复**（注入前实体化）；
  validate 告警路径单测；`_check_*` docstring 重复注释清理；**charts.py 1347 行拆六模块**
  （charts_base/charts_scenario/charts_score/charts_l1/charts_cycle/charts_misc + 兼容壳
  逐名 re-export），**golden 逐字节零漂移**。
- **批次 D 功能**：`scripts/monthly_checkup.py` 月度体检（扫报告目录→到期/临近复盘清单+
  `--cal` 复跑校准 + `--disclosure` 查披露计划）；**triggers 触发条件状态条**（fill 可选字段，
  13 章手写仪表盘前垫状态条：hit=绿/miss=红/pending=灰，评价语义色；非法 status/超 8 条告警）。
- 测试：test_render_core 34 项 / test_golden 3 项 / test_mcap_mode / test_calibration 3 项 /
  test_extract_review 5 项 / test_em_fetch 11 段全绿；golden 两次有意重建（双新图、
  触发条件条占位符+CSS），两次拆分环节零漂移验证。

### V4.8.3（2026-09-03）— 全面审计修订版（thermo-nuclear 审计五项落地）

thermo-nuclear 级代码质量审计（artifacts/v4.8.2-全面审计报告.md）发现的 5 项动作全部落地，
行为零变更（渲染产物逐字节一致），结构与运行效率双修：

- **em_fetch 缓存键实锤修复**：`fetch_pe_pb_band` 曾把 `fields` 塞进 params——`ts_call`
  键归一化剔除 `params["fields"]` 后，与 E2 月末 PE 回填（走第三参、键尾为字段串）永远不同键：
  透明缓存永不命中、冷缓存每股多打一发请求、且请求不带 fields 全字段拉 5 年 daily_basic
  （负载约 5 倍冗余）。v4.8.3 起 fields 归位第三参（两处窗口同为 5 年、键真合一），:766
  注释改为失实已修后的准确表述；磁盘缓存写入 tmp 名加 PID 后缀（防并发进程同键互踩）。
  `test_em_fetch.py` 新增第 11 节锁定「daily_basic 的 fields 必须走第三参」。
- **渲染器模块化拆分 + SVG 基座**（2815 行单文件 → 四模块，主模块 853 行）：
  `scoring.py`（475：共享基座/判词徽章/评分计算/卡片族）← `charts.py`（1055：色板常量 +
  几何工具 + 全部图表）/ `validate.py`（531：22 个 _check_* + validate_content）←
  `render_report.py`（853：主流程，子模块全量再导出，`import render_report as R` 按名访问
  零破坏）。依赖单向无循环。SVG 基座：17 个颜色常量收编全文 135 处散色值；4 个 helper
  （`_svg_open`/`_svg_close`/`_vgrid_ticks`/`_anchor_fit`+`_anchor_clamp`）合并 10 处逐字
  骨架、5 处网格刻度循环、5 处锚点避让分支——分支顺序/钳位语义逐一比对，差异处不强行合并。
  **逐字节验证**：v481/v482 两份冒烟 fill 重构前后渲染产物 diff 为空 + golden 零漂移 +
  三个渲染测试文件零改动全绿。
- **fill-schema 瘦身**：删除渲染器已兜底的教学条文——表头对齐配对（渲染器自动修正）、
  情景表方向（自动转置）、gap 对照表列对齐（自动完成），各留一句指向「表格」节；新增
  「数据直填第一原则」总纲（凡有 em_fetch 出处的数字一律照抄落盘/摘要回填、禁手估），
  把散在各字段行的直填纪律收成一条入口规则。
- **规则权威化（消除数字双写）**：金融子类六维权重（6/8/18/22/24/22）从 `scoring.md`
  移入 `industry-financials.md` 开头成唯一出处（含 1D/1F 权重理由），`scoring.md` 改指针；
  层占比数值表唯一保留在 SKILL.md Phase 0.5 分型表，`scoring.md` 删表改指针；周期行业
  枚举两处措辞统一。口头强化类规则（审计意见属红灯 a 的点位提醒、黄灯上限在清单/字段行/
  硬校验清单的复现）有意保留——它们是易错点的守门提醒，单一权威已标注。
- **闸门「拒/警」分级对齐**：SKILL.md「渲染后检查警告」重写为与渲染器行为一致的两档
  （valuation/scenarios 缺失必须修复重渲无豁免；peers_plot 缺失补字段或合规降级
  matrix-table 兜底）；自检清单 peers_plot 条目「静默跳过」失实表述改「触发告警」；
  position 单元格 ≤15 字写作口径 / 渲染器 >40 字告警阈值的两级口径 fill-schema:75
  本已写明，无需改动。
- 回归：六套测试全绿（render_core 22 / golden 3 / mcap / em_fetch 11 节 / extract 4 /
  calibration 3）；RENDERER_VERSION=v4.8.3。deploy.ps1 为目录级 /MIR 同步，新增三模块
  自动随部署，无需改脚本。
- 审计发现本轮未做的项（记 debt）：`_em_f10` 成功路径双拉（可 size=12 合一）、daily
  单日/430 天双窗口（可合并）、E1 串行仅东财兜底必要（可入池省 4-8s）、`_TS_CACHE` 无锁、
  `fetch_forensic` 层越界返回成品文案。

### V4.8.2（2026-09-03）— 中国联通报告 6 项反馈：龙卷风长名溢出 / 业务构成改横条 / 分位区人话 / 发丝图美化

中国联通报告复核暴露 4 项图表问题 + 1 项术语费解 + 1 项机制误解，逐项核实后修订（方案经用户讨论拍板）：

- **龙卷风图变量名溢出**（联通实证：长变量名越出画布左缘被裁）→ 根因：变量名列右缘
  NL=230 写死、名称无宽度检测。v4.8.2 起先组文本行用 `_text_w` 量宽，超 388px 的长名
  按视觉宽度对半折两行（新增 `_wrap_label`）；NL 在 230–400 间自适应，X0/HALF 随动
  （短名布局与 v4.8.1 逐像素一致）。
- **业务构成图改横条**（联通实证：竖柱下长分部名单行居中互相挤压；H=360 写死且占比集中
  时上半幅全空）→ 横条=收入占比（0-100% 定域不变），左列名称（宽度自适应、超 200px
  折两行），钢蓝圆点=毛利率（同一 % 轴，用户问"横条后毛利率怎么办"的拍板答案：圆点编码
  保留不降级为文字），右列固定毛利率数值列；高度随行数自适应（3 行 H=230 取代 360）。
- **PE 历史带 P25–P75 标签改人话**（用户反馈看不懂「（P25–P75 17–20.8x）」）→ 行内标签
  改「（一半时间落在 17–20.8x）」，source 注保留 P25–P75 术语并补「历史上一半时间的 PE 落点」。
- **股价/PE 历史发丝图原位美化**（用户拍板原位美化、不拆双面板）→ 股价线下浅沙色面积
  填充、发丝 1.3→1.8、年份 tick 加竖向浅网格、末端最新值标签加药丸底色。
- **同业对比「没触发」→ 机制误解，非 bug**：同业章（Phase 4）每份报告都生成、数据现取；
  `extract_review.py --find` 只在同代码旧报告存在时触发回测。「旧同业报告复用」功能不存在，
  用户拍板暂不做（再想想）。
- 回归：test_render_core 22 项（新增 test_tornado_long_names；segments/发丝图/分位区断言
  更新）、test_golden 3 项（快照无漂移——minimal fill 不含可选图）等全绿；
  长名冒烟渲染 artifacts/v482_样式预览.html 浏览器目检四图通过。

### V4.8.1（2026-09-03）— 比亚迪复盘报告 9 项修订：图表视觉压缩 + 章节对调 + 四拍改名

比亚迪-002594 复盘报告（2026-09-03）暴露的 9 项问题，逐项核实后修订（方案经用户讨论拍板）：

- **龙卷风图视觉对齐**（反馈：字大柱宽图高）→ 根因：画布 W=860 与其余图表 1000 不一致，
  容器等宽显示下被放大 ~16%。W 统一 1000，柱高 22→17，行高 56/46→44/36，底部留白压缩。
- **四拍末拍「收口」改名「评分」**（比亚迪 3.1/3.2/3.5/4.1-4.3 用 `.verdict` 灰色小字、
  3.3/3.6 用「收口：」正常段落，混用且内容重复实证）→ 末拍统一为正常段落
  「评分：X.X——一句理由」（分值与本块徽章一致），禁止 `.verdict` 小字充当末拍；
  fill-schema 维度块骨架与四拍正例、render 四拍挤段检查同步改口径。
- **业务构成图改柱线组合**（用户拍板方案 B，否决了"收入额柱+截断+饼图"双图方案——
  截断主数据柱条破坏"长度=数值"契约；占比定域恒为 0-100% 天然免截断）→
  竖柱=收入占比 + 钢蓝折线=毛利率（同一 % 轴，免副轴），柱端=占比、柱下=分部名+收入/毛利额；
  折点在柱内时标签改柱内下方（蓝柱白字/灰柱深字，防蓝底蓝字隐身）。
- **产业链图直线改贝塞尔曲线**（水平进出 S 弯），`self_note` 移到公司卡正下方居中——
  比亚迪"整车+电池+半导体垂直整合，自供比例行业最高"塞不进卡片的实证修复。
- **PE 历史带显示域自适应截断 + 里程碑标签双层交错**：hist_hi 超决策值集合
  （合理带/当前值/关键时点）×1.5 时截到该倍数，右缘虚线封口+「峰值→」标注，
  全量区间仍在带上方文字给出（比亚迪 17–286x：286x 系 2021-22 TTM 净利极薄期真实数据，
  tushare daily_basic 口径无错，问题在显示）；v4.7.1 遗留"标签单层排布"debt 就此做掉。
- **股价/PE 图 PE 序列结构性修复**：比亚迪 fill 只有股价没有 PE 的根因=PE 须手工
  「月收×总股本÷TTM净利」逐月回填被略过 → em_fetch E2 月线直接输出月末 PE(TTM)
  （daily_basic 与 PE 带同参同字段，命中透明缓存不增发请求；失败静默降级仅收盘），
  fill 照抄即可，手工推算口径废除；图高 300→240（用户反馈"傻大傻大的"）；
  图注补判读方法（价涨 PE 平=业绩驱动；价平 PE 跳=估值重定价——比亚迪 2026Q1 财报日
  TTM 净利 -15.5%、价不动 PE 原地 +18% 的台阶实证）。
- **三轨哑铃图加方向箭头**：连线末端箭头指向上版→本版（绿=上调、红=下调），
  两点过近（<26px）不画。
- **章节 12/13 对调**（用户逻辑验证成立：跟踪仪表盘内容=新指标表，是复盘的产出物，
  叙事应先复盘后跟踪）→ 12=回测复盘（条件）、13=跟踪仪表盘；模板/侧栏目录/
  fill-schema 章节编号/backtest/README/data-sources/测试断言全链路同步。
- **跟踪仪表盘两表合并**（用户拍板）：原「关键跟踪指标表 + 触发条件表」并为
  「跟踪与触发」单表——指标 ｜ 当前值 ｜ 触发阈值 ｜ 触发后操作 ｜ 数据来源 ｜ 更新频率。

回归：test_render_core 21 项 / test_em_fetch 10 节（+1：E2 月末 PE 回填与降级）/
test_golden 3（快照有意重写：模板章节对调）/ test_extract_review 4 / test_calibration 3 /
test_mcap_mode 全绿；RENDERER_VERSION=v4.8.1。冒烟：`artifacts/v481_样式预览.html`。

### V4.8（2026-09-02）— 业务构成/产业链两张新图 + 防伪链闭环 + 图表候选四小件

- **业务构成图（3.1 赛道与宏观，用户追加）**：fill 新增 `segments`（可选），脚本生成横条图——
  条长=收入占比（0-100% 定域），首条钢蓝=第一大业务，右列=收入·毛利率·毛利与毛利占比。
  **口径定案（用户拍板）**：分部净利润无公开数据源（tushare/东财/妙想均只到毛利），
  利润维度一律用毛利额/毛利占比并在图注明示，禁止编造分部净利。
  配套：em_fetch `fetch_mainop` 回填 GROSS_PROFIT（tushare 取 bz_profit、缺则收入−成本；
  东财折算），E6 摘要行补毛利额/毛利占比；**fina_mainbz 同源改名重复条目去重**
  （「烯烃产品」与「烯烃」收入/成本全等双行，宝丰 2026-09-02 实证——签名=（收入,成本）留先发行）。
- **产业链位置图（3.2，用户追加）**：fill 新增 `industry_chain`（可选），脚本生成三栏流向图
  （上游行业→本公司钢蓝卡→下游行业，发丝线=供需关系）。**只列行业不列企业**（用户定案：
  上下游玩家众多，列企业以偏概全；无 API，年报/妙想 notice/10jqka 定性检索回填）。
  两图挂法：l1_html 注释锚点 `<!--SEGMENTS-->`/`<!--CHAIN-->`——锚点缺失图追加章末+告警，
  字段未填锚点静默清除（与第 10 章裸占位符同款思路，不碰模板条件块嵌套限制）。
- **防伪链闭环（方向一 + 遗留 0，handoff 既定下一轮首选）**：
  ① em_fetch `--out` 落盘新增 `timing`（现价/MA60/MA120/52 周高低，tushare daily 日线计算，
  港股复用 hk_daily 缓存）——技术面六信号数据源唯一化（神华假 MA60/假月线同源修复），
  SKILL.md/自检清单成文「时机信号一律取自落盘，禁止手估」；
  ② render `_check_quote_consistency` 扩展四件套：`valuation_inputs.pe_ttm` 与落盘偏差 >1% 拒；
  `pe_band` 与落盘历史带**完全无交集**拒（部分重叠正常）；`risk_free`/`div_yield` 偏差 >0.3pct
  告警不拒（港股 div_yield 税后折算差异天然大，不机械比对）；行业口径（metric_label）跳过 pe 比对。
- **图表候选四小件**（handoff 候选 1/2/3 落地）：
  ① `price_history` 股价/PE 历史发丝图（第 10 章，历史带之后时段拆解之前；E2 月线同源，
  双轴：左股价深灰/右 PE 钢蓝；<12 点不生成；与 pe_history 同分型绑定规则）；
  ② `pe_history` 新增可选 `p25`/`p75`（带内深沙分位区，E1 落盘 pe_p25/pe_p75 直接引用；
  em_fetch `fetch_pe_pb_band` 同步输出分位点）；
  ③ `consensus` 卖方一致目标价带（7 章走廊灰带叠加，E5 目标价区间回填）；
  ④ `holders` 股东户数趋势图（11 章时机判定小表后，E4 回填 ≥3 期；
  环比按筹码语义着色：户数增=分散红、减=集中绿）。
- **渲染告警**：`_check_optional_charts`——segments 占比和偏离 100% 超 5pct 告警；
  industry_chain 上游/下游缺一告警；price_history 已填但 cycle_html 缺失告警（同 pe_history 绑定）。
- **测试**：test_render_core 17→21 项（两新图锚点/告警、发丝图+户数图位置断言、
  走廊灰带+分位段、四件套防伪正反例）；test_em_fetch +2 节（E6 毛利额回填+去重、
  P25/P75 与时机素材精确值断言）；golden 快照**未变**（新占位符同行裸替换，空串无残留，
  最小 fill 输出逐字节不变）；em_fetch 600989 实跑验证 E6 四行占比和 99.9%、
  时机素材/分位区/落盘 JSON 字段齐全；扩展字段全量冒烟渲染（宝丰 fill）位置断言全过。

### V4.7.1（2026-09-02）— 东方电气报告反馈 10 项修订

- **PE 历史带图挪至第 10 章周期规律**（本轮核心，经 grill 对齐）：历史带图（概览）垫在手写
  时段拆解表（明细）之前，同一份 E2 月线 PE 序列取数。定案依据：中国移动 600941 报告实证
  第 10 章实践中已是「估值时段拆解章」（触发条件=PE 波动 >50%，表格列含 PE，可复用规律即
  估值锚结论），schema 文字此前停留在商品周期形态、滞后于实践。**分型规则成文**：周期股/
  稳健成长股/稳定价值股/困境反转股——第 10 章必写 + `pe_history` 按数据可得性填写；
  快速成长股/未盈利股——禁填 `pe_history`（历史区间锚不住合理带），第 10 章时段表仍可写
  （描述性）。**7/10 分工**：相似时段 PE 对照的完整推导住第 10 章，7 章校准 info-card 只写
  结论并引用（消除东方电气报告两处重复展开）。模板条件块不支持嵌套 → 第 10 章内
  `{{PE_BAND_HTML}}` 用裸占位符（空串替换无残留）。联动：fill-schema（pe_history/cycle_html/
  valuation_html）、scoring.md（PE multiple 推导落位）、data-sources.md（milestones 采集）、
  SKILL.md（Phase 5/checklist）。
- **`pe_history` 新增 `milestones` 可选字段**：3-6 个关键时点（峰值/谷值/典型时段），渲染为
  带内灰虚刻 + 时点 PE 标签（画布上沿单层排布，debt: pe 过近会叠，真叠时升级双层交错）。
- **目标价走廊中枢标签统一移条上方**：删除「条外右侧放不下→条内白字」分支（白字与条内
  白色中枢刻交叠、手机上更挤），条上方右缘溢出改 end 对齐防裁切。
- **手机图表可读性**：全部 6 张脚本 SVG 加 `min-width:760px` + `.plot-wrap` 横向滚动
  （手机字号保持 76% 缩放）；龙卷风画布收窄 1000→860、变量名字号 12.5→13、数值 11.5→12。
- **版式修复**：Hero 三轨评分列纵向居中（`.hero-scores-col` + justify-content:center，上下
  空白等分）；`.section-tag` 改 block+fit-content（「分型：XX股」标签独占一行，手写正文直接
  跟在 span 后也被强制换行）。
- **渲染告警三项**（stderr 提示不拒渲染）：① dim-block 四拍挤段（单段含 ≥2 个
  判词/论据/对比/收口拍名 → 提示各自成段）；② 3 年趋势表三年数字并排（连续三年份表头或
  单元格内"年份:数值"堆叠 → 提示改「起→终（方向词）」）；③ `pe_history` 已填但
  `cycle_html` 缺失（图挂第 10 章、整章删除后图无处显示）。
- **文档修订**：fill-schema dim-block 四拍正例改四段各自成段（正例本身就是挤段源头）；
  p0_html 明确 section-tag 独占行；gap_html 对照表补列对齐规范（首列不换行/数值列
  `td.num`/结论列 `td.center`）；3 年趋势表补负面示例（❌ `2023: 8.0｜2024: 15.2｜2025: 30.8`
  并排，✅ `8.0→30.8（大升）`）。
- **测试**：test_optional_charts_render 更新（第 10 章内顺序断言 + milestones 渲染断言）、
  新增 test_writing_discipline_warns（三项告警正反例）；golden 快照更新（CSS/svg style/走廊
  标签位置）。

### V4.7（2026-09-02）

- **报告视觉改版**：暖象牙纸色体系 `#faf6ef`——首轮只改了底色导致白卡片/冷灰表格突兀，
  复审后按 taste-skill 色板纪律全量统一：卡片面 `#fffdf9`、凹陷面（章节头/斑马纹/结论框/
  图表容器）统一 `#f7f2e7`、表头 `#f1ebdd`、蓝色调底纹（质量轨道/info-card/hover/合计行）
  改暖沙 `#f2eddd`、边框 `#e0d7c3`、文字冷灰全族暖化（正文 `#3a362e`/标题 `#2b2620`/
  次级 `#8a8375` 等）；render_report.py 两个 SVG 图表的内联色同步（含批次 1 漏网的
  网格线 `#eef1f5`）。保留：品牌钢蓝 `#4a6fa5` 唯一强调色、语义色（红/绿/橙/警示卡）、
  打印白底。
  删除模板顶部「个股深度分析引擎」顶栏（HTML 块 + 8 条 CSS + render 的 TOP_ICON 映射；
  fill-schema 的 `top_icon` 字段同步删除；日期信息由 Hero「报告日期」保留）。
  **联动修复**：`extract_review.py` 日期锚点原从顶栏 `topbar-tag active` 提取，新增
  Hero 副标题「报告日期：YYYY-MM-DD」回退路径，旧版报告顶栏路径保留兼容。
- **现价防伪通道（神华 601088 现价造假事故修复，P0）**：`em_fetch.py` 新增
  `--out=<path>` 把 E1 源头数字（现价/PE(TTM)/PB/市值/分位带/risk_free/股息率/数据日期）
  落盘 JSON；fill 契约新增 `quote` 字段（`{"source_file","date"}`）；render 校验
  `price`/`pe_ttm` 与落盘值偏差 >1% 即拒渲染（与估值分四件套同级），字段缺失仅告警
  （存量兼容）。现价类数字从此与估值分一样没有手填路径。
- **截断事故与委派边界成文**（data-collection.md）：目标公司 E1 一律主会话自跑并 `--out`
  落盘，子代理只跑 peers 且回传必须含 peers E1 原文；禁止指示子代理删除输出文件；
  回传截断/文件不存在/本体缺失 → 必须重跑，禁止凭记忆补数（与新华保险 `| tail` 事故并档）。
- **千位符渲染兜底**：Hero 金额卡显示层归一（`_fmt_thousands`：10330.7 与 10,330.7 均输出
  10,330.7；price 走 `_num` 解析禁逗号，两类字段待遇文档写明）；**3 年趋势表内容标准**
  入 fill-schema（必选 6 行 + 分型附加行 + 每行必须被结论框引用 + mx 只取 A 股口径）。
- **静默吞错修复（P1-E/F）**：main 入口预检 tushare token 并 stderr 硬告警；
  治理/披露/审计/有息负债 4 函数 6 处裸 `except Exception` 收窄为 OSError 静默 +
  RuntimeError 打印可见；render 主路径补 `except ValueError → exit 2`（不再吐 traceback）。
- **取数效率（P0-A/C/D + P2-L）**：daily_basic 三次调用合并——新增 `_daily_basic_latest`
  （15 日窗口+最小字段）供 fetch_quote/_ts_latest_quarter 共用，消除两处无窗口全量拉取；
  `fetch_risk_free` 按日期模块级缓存；11 个 `_sec_*` 章节并发（ThreadPoolExecutor
  max_workers=4，E1 先跑因 K 线缩放校准依赖，输出保序）；`_tushare_token` 加 lru_cache。
- **跨进程磁盘缓存（P0-B，用户裁决重启）**：tushare 与东财 get() 统一落
  `%TEMP%/em_fetch_cache/`——行情 2h / 财务 12h / 治理 24h 分档 TTL，`EM_FETCH_NO_CACHE=1`
  旁路，写入先 tmp 后 replace 防并发读半文件，读写失败静默不阻断。实测两连跑
  2.3s→0.15s 且输出一致；NO_CACHE 旁路输出一致。动机实证：report_rc 限 1次/分钟，
  连续两跑第二发必降级东财兜底——缓存后该限流不再被报告间重跑消耗。
- 测试：test_render_core 11 项（+quote 防伪、+千位符）、test_em_fetch 7 项（+token 告警、
  +磁盘缓存）、test_extract_review 4 项（+日期双路径）全绿；golden 快照两次有意更新
  （模板改版、千位符归一）；em_fetch 600989 实跑与改动前 diff 为空（E5 曾现差异，
  定位为 report_rc 1次/分钟限流所致，与本轮改动无关，间隔重跑一致）。

**以下为当日下午第二轮追加（图表与版式改版，lieflat-charts 法典审查驱动，赤峰黄金预览走查两轮迭代）：**

- **图表体系改版**：
  - 目标价走廊：竖版幽灵胶囊（fill-opacity 0.14、1000×400 画布三根小柱）→ **横版实心区间条**
    （nice 刻度 + 币种单位注 + 现价竖虚线边缘避让，窄区间不再退化成点）；
  - `_ticks` 等距取整 → nice-number 步长（1/2/2.5/5×10^k），消灭 19/32/44/57/69 类不友好刻度；
  - 同业散点图：分带底色 0.06→0.12、点/标签加深一档、标签四向碰撞避让 + SVG 边界防裁切
    （右缘公司标签曾越界被裁，山东黄金 31.06x 实证，附回归测试）、points 可选 `size` 市值编码（sqrt）；
  - 新增口径告警：`peers_plot` 目标点 PE 与 `valuation_inputs.pe_ttm` 偏差 >30% 提示口径核对
    （赤峰 2026-09-02 实证：图中 A 股 23.58x vs 正文 H 股 18.6x 混排）。
- **新增 4 张脚本生成图**（纯 SVG、类名写死、可选字段驱动、缺失静默跳过不告警）：
  6 章九维评分分布横条图（scores/weights 数据现成，恒生成；条端=得分+判词，右列=权重·加权，
  **替代原质量分明细表**，第 6 章只保留公式 footer）；
  2 章末敏感性龙卷风（`sensitivity`，可选 delta/amount 子行承载变动幅度与金额影响，
  **填了即替代 P0 手写敏感性表**；红=不利/绿=有利，降序，首行=第一变量）；
  7 章 PE 历史带子弹图（`pe_history`，历史区间浅带+合理带钢蓝段+当前黑刻）；
  13 章三轨分新旧哑铃图（`prev`，回测模式专属，连线绿升红降）。
  （财务趋势分组柱曾随本轮开发、走查后删除：与 3.4 财务年表信息重复且装不下 ROE/负债率等维度——
  宁缺毋滥、不损失信息，`fin_history` 字段未入契约。）
- **版式修复**：正文 `p` 零间距 → `p+p` 段距 10px（核心结论四段粘合实证）；
  `.verdict` 判词行加深至 #66604f + 3px 边；Hero 估值卡 s-sub 改四件套公式；
  右缘固定侧栏目录（纯 CSS 零 JS，两字简称 + 悬停 title 全名，宽屏显示、窄屏/打印隐藏，
  条件章节按存在性生成，编号与模板固定章节号一致）。
  （正文 82ch 限宽曾试装、走查后撤销：文字堆左、右侧大面积留白——段距修复保留。）
- `RENDERER_VERSION` 升至 v4.7（上午批次漏 bump，本批一并修正）；golden 快照两次有意更新；
  test_render_core +5 项（nice 刻度/可选图存在性/回测哑铃/口径告警/标签越界回归）。

### V4.6（2026-08-30）

- **golden-file 端到端快照测试**：新增 `test_golden.py`——最小合法 fill 全量渲染与入库快照
  逐字节比对（版本标记归一），渲染器/模板任何非预期输出漂移报红；含快照自证（fill 改动必被捕获）
  与 `--check` CLI 退出码用例（合规 0 / 拒渲染 2）。
- **回测触发宽松档**：`extract_review.py --find [代码] --include-unmarked` 让 pre-v4.3 无渲染器标记的
  旧报告参与回测匹配（同日期带标记者优先，匹配时提示人工核对）；严格档行为不变。
- **北交所取数实测闭环**：东财 secid 前缀 `0.` 对北交所正确（920982 全链路实测通过）；
  同时暴露并修复真实陷阱——83/43 等 2025-10 前旧代码返回「已切换」零值陈旧档，
  `_em_quote` 检出即报错并提示改用 920 段新代码，零值不再静默流入报告。
- **预期差 B 档准入条件**：一致预期覆盖 ≥5 家的标的必须先做研报检索才可落 B 档
  （万华化学 2026-08-26 实证：27 家覆盖却零检索直接反推；08-26 后 12 份报告定性研究采用率 91.7%，
  本条针对的是漏网个案而非普遍缺失）。
- **fill-schema 精简 + 增补**：两个兜底手写骨架（matrix-table / scenario-table）代码块压缩为规则条文
  （30.2K → 28.7K 字符）；新增 dim-block 薄块正例（判词→论据→对比→收口四拍结构样板，
  拉齐弱模型内容厚度，不动地板阈值）。
- **评分统计校准工具化（思考题①）**：新增 `scripts/score_calibration.py`——扫存量报告提三轨分，
  算报告日后前复权实际收益与沪深300/恒指超额，按质量分/估值分分桶（全口径+同股去重两口径）；
  港股日线走东财日 K（tushare hk_daily 限 1次/小时）；旧版单轨报告取文件名分近似、入明细不入桶。
  首跑 41 份观测：估值分桶收益单调（贵 -2.6% → 深度安全边际 +2.1%，短窗描述性）。
  backtest.md 补「每月复跑入档」纪律。
- **巨函数拆分（F9/F10/F19）**：render_report.py `validate_content`（241→31 行本体 + 17 个
  `_check_*`）与 `render`（202→46 行本体 + 11 个辅助）；em_fetch.py `summarize`（305→30 行本体 +
  11 个 `_sec_*`）。纯代码搬移零行为变更——golden 快照逐字节一致 + em_fetch 600989 实跑 diff
  为空双重验证（A1 快照测试本轮先建，正是为此兜底）。
- **SKILL.md 中英混排清理**：全部标题中文化（Phase 0-7、何时使用、产出方式等），
  正文残留的英文段（When to Use 正文/开头简介/NEVER invent 条目）一并中文化。

### V4.5（2026-08-30）

- **决策矩阵质量分 <4 落位 bug 修复**：`_matrix_slot` 返回的字符串「不建议参与」此前未被档位分支
  接住，被渲染成观察池；现落位与判词一致（返回档位 0），附回归测试。
- **`--check` 补估值分计算**：预检口径与正式渲染一致，保证「退出码 0 = 可渲染」。
- **em_fetch 加固**：429 硬停（检出限流即抛错终止，不再原样重试+静默吞错）；市场映射统一
  （secid_of/to_ts_code 共用映射表，顺带修复北交所 92 开头误判为沪市的既有 bug）。
- **必填字段告警升级为硬校验**：timing_scores/yellow_deductions/thesis_html/price/date 缺键即拒渲染。
- **港股币种字段**：fill 新增 `currency`，模板与情景表单位不再硬编码「元」。
- **三情景按 key 取值**（多余 key 不再错位）；年化中枢改用 base 情景级 horizon；
  质量分汇总 footer 红旗改 1D 行内注解（等式成立）；负扣分/PE 倒挂/shares≤0 拒渲染；
  fill JSON utf-8-sig 兼容 BOM；输出覆盖警告；残留检测覆盖小写占位符；死代码清理。
- **文档收敛**：base-rates.md、analytical-tips.md、valuation.md 全部并入 `scoring.md`；
  SKILL.md 数值复述收敛为引用；事故典故括号注迁移至本文件；无条件硬门禁 4 → 3。
- **回测触发脚本化**：`extract_review.py --find [代码]` 机械判定（文件名匹配 + 渲染器标记验证 +
  取日期最新），手写绕行产物不再误触发回测；extract 的 d-item 提取改 None 安全。
- README 重写为产品文档（此前是补丁注记堆砌，版本历史迁入本文件）。

### V4.4

- DeepSeek 全流程实测验收（万华化学 2026-08-26，55 步日志逐条过）：硬门禁文件全读、
  peer≥3 条件委派执行、`--check` 五轮迭代按设计工作、防绕行标记在位、零绕行。
- 引号归一：fill JSON 内嵌 HTML 的 `class='x'` 单引号入库自动归一双引号。
- 委派扩容：妙想 MCP 检索类取数与 `--check` 契约层修复列入「可委派（推荐）」。
- F11 收敛：决策矩阵/档位序列/四件套公式收敛为 scoring.md 唯一权威。

### V4.3

- 防绕行：渲染输出嵌入 `generated by render_report.py` 版本标记。
- 内容地板加厚：thesis 只有目标价 → 告警；dim-block 纯文本 <40 字拒渲染、<80 字告警。
- `--check` 预检模式：fill 落盘后自检的唯一合法方式（解析+评分+地板全预检，退出码 0/2）。
- 取数提速：ts_call 透明缓存 + 财务三表统一窗口。
- 子智能体条件强制：peer ≥3 批量取数 / 公告 PDF 下载必须委派。
- 回测模式加「关键假设变更表」硬要求（防相邻两版评分漂移）。

### V4.2

- 全维度评分锚定：九个质量维度 + 时机分全部从软约束升级为分档锚/清单锚。
- 1D 财务健康改趋势+流动性双轨；1E 治理天花板升至 9.0；离散度调节收紧到极端档。
- 取数自动化：em_fetch 新增中债 10Y 无风险利率、TTM 股息率、资本开支；新增 extract_review.py。
- fill JSON 分段写盘硬规则；渲染器 UTF-8 输出、表格对齐内容投票、极端分证据告警等加固。

### V4.1

- 估值分脚本强制计算：无手填路径（四件套输入 + 结构化三情景）。
- 内容地板硬校验：不达标渲染器直接拒渲染。
- 评分汇总章恢复（第 6 章，脚本生成）+ 11 章顶三轨汇总卡。
- 决策矩阵阈值统一；1D 红旗扣分结构化（red_deductions 字段）。

### V4.0

- 估值独立成轨：质量分（L1 六维 + L3 三维，不含估值）/ 估值分（价格轨）/ 时机分（微调）。
- 主文件瘦身 + 参考文件分层：SKILL.md 只留骨架，细则下沉 references/。
- 数据通道升级：tushare 优先 + 东方财富公开接口兜底；妙想 MCP 直调层。
