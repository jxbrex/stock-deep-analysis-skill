# v4.9.1 交接 — 全面审计修复（残骸清理 + 死面收敛 + 文档归位）

> 触发：用户发现 v4.9 文件名关联历史案例，担忧「失败案例坟场」堆积。
> 经全仓库审计（thermo-nuclear 标准）：无案例数据残留，坟场实为 artifacts/ 的
> 迭代工作残骸（4.1MB，未追踪/不部署/无引用）；另确诊版本标签错位、兼容壳残留、
> re-export 死面、解析器三处漂移等结构性问题。本版全部修复。**未 commit**（用户要求
> 下份报告发现的问题一起改后再提交），已部署。

## 本版变更（三波落地，七套回归全绿）

1. **坟场清理**：artifacts/ 全删（~4.1MB：v4.8-v4.9 调试截图 3.5MB、重构备份
   `_render_report_phase_a_backup.py`/`_em_fetch_pre_split.py` ~260KB、一次性迁移脚本、
   冒烟 fill、预览 HTML、案例报告）+ scripts/__pycache__（528KB）。核查结论：无一字节
   被 git 追踪、deploy.ps1 本就排除、无任何代码引用。
2. **版本标签归一**：29 处「v4.10」误标 → v4.9（合并发布时未清理的开发期命名）。
   模板 CSS 注释变更触发 golden 快照有意重建一次（二次比对通过）。
3. **charts.py 兼容壳删除**（42 行）：render_report/validate 直导真身模块；
   render_report 三段 noqa F401 再导出 96 名 → 7 名（= 测试经 R.* 消费的全集，
   块上注释已如实标注「供测试经 R.* 按名访问」）。
4. **em_fetch re-export 收窄**：40 → 21 名（19 个纯挂名零消费实证删除）。
   文档 `em_fetch.py::私名` 前缀改指真身 em_data.py（data-sources.md:12、scoring.md:150；
   scoring.md:146 的 red_flags() 真身本就在 em_fetch，不动）。
5. **文件名解析合并**：owner = `extract_review.parse_report_name`（三代命名 + 港股 5 位）。
   score_calibration/monthly_checkup 删本地实现改复用。修复性变化：monthly 自此识别
   回测版与旧 _ 命名（旧正则漏判）；extract_review 旧 _ 命名参与回测触发（原宽松档
   名存实亡）。
6. **--disclosure 修复**：删 startswith("6") 手拼 ts_code（港股/北交所静默拼错），
   改复用 `em_fetch.fetch_disclosure`（to_ts_code 市场映射）；港股显式跳过。
   顺带修复同审查日多标的 `overdue.sort()` TypeError。两处「口径待实测」注释删除。
7. **helper 去重**：`_prev_track_rows` 共享实现落 charts_base（validate/charts_misc
   同一旧键回退契约合并）；`_em_kline_url`（klt 参数化）收编两处 URL 拼接；
   `_C_PAPER_CELL`/`_C_BADGE_DEEP` 收编三处散色值（逐字符不变，golden 零漂移实证）；
   extract_review 删本地 `_num` 改从 scoring 导入。
8. **死物清理**：charts_base 死 import sys、score_calibration 死 import datetime、
   test_mcap_mode 死变量 SKILL_DIR、deploy.ps1 删 .backup-pre-v48 排除项。
9. **文档补齐**：SKILL.md 补 fin_trend/growth_plot（必填，缺失拒渲染）+ triggers（可选，
   13 章仪表盘前）两处；README 目录树 7→24 项、测试「四个」→「七套」；
   backtest.md 6.5 增「月度体检」小节（消除 monthly_checkup 文档孤儿）。

## 测试现状

七套：test_render_core(34) / test_golden(3，字节比对) / test_calibration(3) /
test_extract_review(5→7) / test_monthly_checkup(新增 2) / test_em_fetch(11 段) /
test_mcap_mode(冒烟)。全绿。golden 本版有意重建一次（模板注释），此后比对通过。

## 审计中评估但**不拆**的项（下一位别再提）

- **em_data.py 1064 行过 1k 线**：高内聚取数簇（跨组共享缓存/转发 stub/_em_dc），
  按 E 编号切文件必复制共享设施；test_em_fetch 的 rebind 语义是硬约束。仅超线 6%，保留。
- **handoffs/ 保留**：AGENTS.md 约定有意保留（维护史），抽查 v4.8↔v4.8.1 相似度 0.01，
  无重复文件。deploy 已排除。

## 遗留观察（不紧急）

- 测试对渲染器私名的强耦合（R._ticks/R._text_w 等）是 render_report 保留 7 名
  再导出块的根因；若未来想彻底消掉，应让测试直导真身模块而非经 R.*。
- `em_data.py` 是 CRLF 混合行尾文件，Edit 工具偶发无法匹配，需字节级替换时参照
  本版做法（python 精确替换 + count 断言）。

---

## 附录：报告生产 25 分钟提效分析（未实施，下版取证后立项）

**时间去向假设（未实测，下份报告须计时取证）**：
1. Phase 1 定性检索串行：10-20 次 WebSearch/MCP 往返压在主会话；
2. fill JSON 流式产出 ~10-15k token（分段 ≤8k/次纪律），数分钟纯输出；
3. render --check 修复循环（万华实证 5 轮）；
4. 回测模式 = 独立取数打分 + 读旧报告 + 全量重写，工作量翻倍。

**「多代理并行写 fill」（tower 模式）评估——方向对，不能直接做**：
- fill 有跨字段硬一致性门禁：thesis 三情景价与 valuation 偏差 >2% 拒渲染、评分字段
  联动、表格数与 .source 配平——多代理各写一段必互撞；
- data-collection.md 明确禁止下放定性写作（弱模型塌陷区，历史事故立规）。
- **可行变体**：主会话先写「契约核心」（scores/valuation_inputs/valuation/时机小表等
  一致性敏感字段），纯机械片段（peers 表、跟踪表骨架）并行下放，脚本合并。
  前提是把 fill 从「单文件分段 append」改为「多片段文件 + 合并脚本」——
  render 管线结构性改动，建议 v4.10 单独立项。

**下份报告取证建议**：执行时记录各 Phase 起止时间（Phase 1 取数 / 定性检索 /
Phase 2-6 分析 / fill 写盘 / render 修复循环），用数据定位真坑再设计。

---

## 补充修订（2026-09-05）— 药明康德复盘报告 8 项反馈

> 触发：用户评审药明康德（603259）回测复盘报告，提出 8 项展示层/规则层问题。
> 评审方式：grilling 逐条过堂 + 三 explore 子代理取证，4 项用户拍板（配色语义 /
> 3.6 表达 / 户数图位置 / 旧核对表存废），4 项按设计结论直接执行。
> 版本仍为 v4.9.1（本版未定稿，此为补充修订；RENDERER_VERSION 常量保持 "v4.9"）。

### 变更清单（对应 8 项反馈）

1. **fin_trend 图墙第 4 面板 + 扣非柱**：标准 4 面板改为 营收×毛利率 /
   归母净利+扣非净利×净利率×ROE / 现金流×现金含量 / 应收+存货周转天数（纯双线）。
   短债覆盖率降为可替换可选面板。配套数据管道：E3 年表新增「扣非净利亿」列
   （`_compose_annual_row` 加 KCFJCXSYJLR：tushare profit_dedt 主源、东财 F10 兜底）
   与「周转天数（应收/存货，天）」逐年行（旧→新，供照抄）。渲染器与 validate 层
   面板受理条件由 `bars and lines` 放宽为 `bars or lines`（纯线面板只出右轴、
   网格随线域、头行只列线末值）。
2. **钢蓝色语义统一**：取消单柱「最新年钢蓝」高亮（钢蓝一身三职：单柱最新年 /
   双柱第二系列 / 线第一系列，药明报告实证混淆）。柱色只表系列归属
   （沙=第一系列/钢蓝=第二系列）；最新年信息由头行最新值+同比（m-yoy）与
   x 轴最右位置承载，无信息损失。
3. **3.5 治理与资本配置分块列示**：复用 `.trig-strip` 类族（零新 CSS），
   3-5 块（股权与控制权/管理层与激励/分红与回购/资本运作记录/风险点），
   状态色语义 hit 绿=正面/miss 红=风险/pending 灰=中性。纯 fill-schema 写作规范改动。
4. **3.6 资本回报质量三卡（不做图）**：块首 metric-row 三卡（ROIC 近 3 年均值 /
   WACC 8% / ROIC÷WACC 覆盖倍数）。否决独立 SVG 图：ROIC 不在 E3 管道，
   单开数据链不值；scoring.md 1F 补分型特例（未盈利/管线与困境反转豁免
   ROIC vs WACC 主判据；金融股已有行业附录口径）。
5. **当前指标表 best/worst 高亮**：新增 `.cell-best`/`.cell-worst` 淡底类
   （淡绿 #e5efe3 / 淡红 #f9eeea）。坑：斑马纹 `tbody tr:nth-child(even) td`
   优先级会盖掉普通类——用三元素选择器且声明在斑马纹之后。渲染器
   `fix_table_alignment` 只动 num/center，高亮类存活（有测试）。
6. **户数趋势图移至 11 章章首**：模板占位符换位（HOLDERS 移到 POSITION_HTML 前），
   阅读流=证据→判定→结论，与第 10 章图垫章首先例一致。
7. **回测模式废止手写旧触发条件核对表**：真正的重复方不是 R 章节（互补），
   而是 v4.9 新增的 triggers 状态条（fill-schema 本就写明「回测模式填旧触发条件
   核对结果」）与 backtest.md:93 老规则撞车。核对结果全部由 triggers 承载
   （target 写「阈值｜实际值」）；render_report `_check_backtest_flags` 新增
   prev 已填而 triggers 缺失告警（不拒渲）。
8. **13 章合并单表加 `.dash-table` 类**：指标/当前值/触发阈值列 nowrap，
   触发后操作/数据来源长文列 12px；schema 强制 table-scroll+dash-table+freeze-first。
   列契约（6 列）不动。

### 测试与验证

- test_render_core 34→36：户数图章首断言改写、单柱无钢蓝柱断言、纯线面板形态
  （无 rect/双线/头行只线末值）、cell-best 类经对齐修正存活、回测 triggers 告警
  有无两分支。minimal_fill 升级：扣非双柱 + 纯线第 4 面板 + dash-table +
  cell-best/worst + 默认含 holders（golden 覆盖新形态）。
- golden 快照有意重建（模板 CSS/图墙配色/图注/s11 顺序），重建后逐项标记核验通过。
- 七套回归全绿；E3 实跑 603259：扣非净利列（2025 年报 132.4 亿）与周转天数行
  （应收 65→60 天/存货 122→120 天）非空。
- 示例文档：`artifacts/_make_demo.py` 生成 `_fill_v491_demo.json`（minimal_fill +
  3.5 trig-strip 分块 + 3.6 三卡 + 回测 prev/review/triggers），渲染产物
  `artifacts/测试股份-600000-7.00-5.5-复盘-2026-09-05.html`，11 项形态标记核验全过。

### 备注

- 本仓库工作区在本周前已有 v4.9.1 全部未提交变更（见上文「未 commit」），
  本次补充修订与之同批提交。
- em_data.py 是 CRLF 混合行尾文件，Edit 工具无法匹配跨行锚点——沿用本版既有做法
  （python 字节级精确替换 + count 断言，脚本落 artifacts/ 用完即删）。
- 药明康德旧报告不重渲（fill 已删）；下次 603259 再分析自动应用新规范。

---

## 补充修订二（2026-09-05）— 科大讯飞报告 4 项反馈

> 触发：用户评审科大讯飞报告，提出 4 项展示层问题（核心结论不落地 / 3.5 换行浪费空间且
> 评分依据不显 / 同业表方向不统一且高亮太弱 / 红灯检查不够醒目）。
> 方案经计划模式评审：核心结论在「2×2 结论卡」与「四段+索引行」两案中，用户拍板结论卡。
> 版本仍为 v4.9.1（本版未定稿，此为第二批补充修订；RENDERER_VERSION 常量保持 "v4.9"）。
> 改动全部落在模板 CSS + fill-schema 写作规范 + validate 告警三层，render_report.py 主逻辑零改动。

### 变更清单（对应 4 项反馈）

1. **核心结论 → `.concl-grid` 2×2 结论卡网格**（模板新增 `.concl-grid`/`.concl-card`/
   `.concl-head`/`.concl-ref`）：四卡固定序 row-major（关键优势/关键弱点/当前市场认知/
   核心投资逻辑），每卡=卡头 + 2-4 句浓缩要点（仍带关键数据、弱点不回避）+ 卡尾
   「详见 X 章」锚索引（固定映射：优势→#s3／弱点→#s5（财务弱点可并引 3.4）／市场认知→#s8／
   投资逻辑→#s7·#s11；只链无条件章节，不链 s10/s12）。浓缩省略的信息由被引章节承载。
   红灯 danger-card（脚本注入）与情绪 warning-card 位置不变（卡格前/后）；
   内容地板 ≥200 字与禁 metric-row/评分表红线保留。validate `_check_conclusion_structure`
   改按四卡文案（纯文本关键词+顺序校验对容器不敏感，逻辑不动）；窄屏（≤768px）卡格落单列。
2. **3.5 治理分块单行化 + 评分依据链**：fill-schema 收紧为每块=单个 `.trig` 行
   （dot+cond+mt+status 全内联，论据压进 `.trig-mt` 一句；禁止 trig 后另起 `<p>` 正文；
   状态含 pending 待验证永不单独成行）；整块 `<p>` 恰为 2（判词段+评分末拍段）。
   评分末拍强制依据链：「评分：X——基准 X.X，加分项（块名+0.x）、扣分项（块名−0.x，
   引用块内数据）」。validate 新增 `_check_governance_strip`：3.5 dim-block 内
   `<p>` >2 → 告警（未用 trig-strip 的存量 fill 不打扰）。
3. **同业两表公司强制=行标题**：当前指标表与 3 年趋势表统一为公司=行（首列公司名+
   freeze-first，指标为列；趋势表必选 6 行→必选 6 列固定列序）；目标公司蓝粗标在行首格。
   cell-best/worst 改按指标列标注，并由「仅染淡底」升级为**染底+同色加粗**
   （best #d9e8d0/#2f6b33、worst #f5dbd1/#a8432f；原淡底 #e5efe3/#f9eeea 对比度过低实证；
   选择器仍声明在斑马/hover 之后）。validate 新增 `_check_peers_orientation`：
   蓝 style 落 `<th>` 表头（=公司列旧方向）→ 告警。
4. **红灯检查 → hero 三卡**（5 章章首固定，模板新增 `.flag-hero`/`.flag-hero-card`/
   `.fh-name`/`.fh-verdict`/`.fh-desc`）：卡面=红灯名（a 财务造假与极高杠杆 / b 立案调查与
   重大违规 / c 主营不可逆衰退）+ 大字 通过/不通过（pass 绿底绿字 / fail 红底红字）+
   小字核查路径或命中事实；命中与顶层 `red_flag` 一致，1 章 danger-card 脚本注入不变。
   分型豁免（金融/困境反转）在卡面小字注明依据。窄屏落单列。

### 测试与验证

- test_render_core 36→38：新增 `test_governance_strip_compact_warns`（紧凑放行/另起 `<p>`
  告警/未用 trig-strip 不打扰三分支）与 `test_peers_orientation_warns`（th 蓝 style 告警/
  行首格放行）；`test_conclusion_structure_warns` 改写为四卡形态（齐全放行/缺卡点名/乱序告警）；
  minimal_fill fixture 全面升级四形态（结论卡/3.5 依据链/peers 公司行/l4 flag-hero）。
- golden 快照有意重建（模板 CSS + fixture 形态变更），重建后逐项标记核验
  （concl-card/fh-verdict/cell-best 新色值/trig-cond/详见锚/`<th>公司</th>` 均在）。
- 七套回归全绿；artifacts/_make_demo.py 3.5 评分末拍改依据链示范，demo 重跑
  形态标记核验全过（concl-grid/flag-hero pass×3/依据链/公司行/cell-best-worst）。
- 科大讯飞旧报告不重渲（fill 不在仓库）；下次分析自动应用新规范。

---

## 补充修订三（2026-09-06）— 美图 01357 港股报告 5 项反馈

> 触发：用户评审美图公司（01357.HK）报告，提出 5 项问题（1 项数据正确性 bug +
> 1 项数据源通道 + 1 项核实后无需修正 + 2 项展示层）。版本仍为 v4.9.1（未定稿，
> 第三批补充修订；RENDERER_VERSION 保持 "v4.9"）。

### 变更清单

1. **港股日线排序 bug 修复（本批唯一数据正确性问题，根治于源头）**：tushare
   `hk_daily` 返回倒序（新→旧），`_hk_daily_series` 未排序直接进缓存——
   `fetch_timing_material` 港股分支尾部切片（closes[-60:]/[-250:]）吃到最旧数据，
   MA60/52 周高低全错（美图实证：算在 2020 年数据上；用户已用同一权威序列手工重算
   纠正落盘 4.95/4.34/4.53/11.82/3.61）。修复：`_hk_daily_series` 缓存前统一按
   trade_date 升序（em_data.py:67）。三消费方免疫复查：fetch_quote 取 max(trade_date)、
   fetch_kline_monthly dict 按月覆盖+sorted 输出、fetch_timing_material 即修复目标。
   A 股分支本就带使用处排序（:1046），不动。
2. **失败哨兵 + timing stderr 告警（同日实测连带加固）**：端到端验证时实测 hk_daily
   限流已收紧为 **1 次/小时**（HTTP 与 MCP 通道同档，此前「1 次/分钟」记录作废——
   score_calibration.py:108 既有注释本就这么写，文档互搏就此归一）。失败不写缓存的
   旧形态下，同进程 E1/E2/timing 三消费方各自重试、三连撞限流。`_hk_daily_series`
   失败改**写空哨兵**（消费方命中即走各自降级：quote/月线落东财兜底、timing 落 None），
   `fetch_timing_material` 裸 except 改 stderr 告警（`⚠️ fetch_timing_material(code): ...`，
   参照 fetch_debt 同款）——限流/失败不再静默吞成 None。data-sources.md 限流预算段、
   data-collection.md 港股段同步修正。
3. **妙想 skill 替补通道写入降级链**：用户新部署 mx-data/mx-search/mx-xuangu 三个
   user-scope skill（同一东财妙想 API，密钥配在 skill 内），作为妙想 MCP
   （`mcp__mx-ds-mcp-stdio__mx_*`）缺席时的等价替补：定量→mx-data、定性检索→mx-search、
   选股→mx-xuangu。SKILL.md 工具检查硬门禁改三级判定（MCP → mx skill → 显式标注降级），
   标注语句相应分两态（「妙想经 mx-data/mx-search skill 通道」/「妙想 MCP 缺席，
   已走降级路径」）；data-sources.md 妙想段+降级链图+限流段、data-collection.md
   工具探测/定性通道表/港股 E3/尾部注同步。口径纪律不变：半结构化、模型直调层、
   不进 em_fetch.py 脚本。
4. **3.5 治理 trig 条目名定宽对齐**：`.trig-cond` 加 `flex: 0 0 7em`（≤7 字单行、
   超长自动换行，行间 mt 列始终对齐——美图「大股东持续减持」7 字与标准 5-6 字块名
   混排实证难看；6em 首版会断出孤字，demo 目检后放宽到 7em），`.trig-status` 加
   `flex-shrink: 0`。同类族 13 章 triggers 状态条同步受益（cond 多为 4 字）。
   fill-schema 补「cond 条目名 ≤7 字为宜，超长自动换行左对齐」。
5. **3.5 重点事实深色加粗**：新增 `.trig-mt strong { color:#2b2620; font-weight:700 }`
   （灰字论据中金额/时间窗/比例跳出，与 cond 同色）；fill-schema 补写作规范
   「trig-mt 内关键事实用 <strong> 包裹，每块至多 1 处」（美图实证目标句：
   「累计套现超 11 亿港元，且发生在业绩预盈公告后十日等高位窗口」）。

**核实后无需修正**：财务健康图墙缺第 4 面板（应收+存货周转天数）——港股 HKF10
接口不提供周转天数字段（data-sources.md:324）且互联网附录已用经调整利润背离/投资
减值/商誉替换应收/存货口径（industry-internet.md:24），双重不适用；3 面板 ≥3 过
拒渲染线，设计内裁剪。

### 测试与验证

- test_em_fetch 新增 9b 段：hk_daily 倒序输入 → MA60=270.5/MA120=240.5/52周窗口
  [51,300] 精确断言 + 月线聚合消费方免疫 + 失败哨兵（两次调用只发一次网络）。
  七套回归全绿。
- golden 快照随模板 CSS 有意重建一次（第三次先例），diff 确认漂移仅 trig 三行 +
  段注释，重建后标记核验（`flex: 0 0 7em`/`.trig-mt strong`/`flex-shrink: 0`/段注释均在）。
- demo：_make_demo.py 的 3.5 块补 7 字条（大股东持续减持 + <strong>11 亿港元）实证形态，
  重渲 `artifacts/测试股份-600000-7.00-5.5-复盘-2026-09-05.html` 供目检。
- **端到端遗留**：美图 01357 真实 hk_daily 比对（对用户纠正值）因 1 次/小时限流窗口
  耗尽未完成，待窗口恢复后跑 `python scripts/em_fetch.py 01357 --out=...` 核对
  timing 五值；排序正确性已由 9b 单测精确覆盖。
- 美图旧报告不重渲（fill 不在仓库）；下次港股分析自动带修复。
