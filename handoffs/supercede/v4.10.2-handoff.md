# v4.10.2 交接 — Hero 分数定性化 + 账本代码批/测试批落账

> 触发：用户三议题——①Hero 质量/估值分下「本质 85% + 预期 15% − 黄灯 1.0」与
> 「中枢·赔率·倍数·股息 四件套加权」两句是废话，应为分数定性（如「中上」「合理无安全
> 边际」）；②完成 v4.10.1 handoff 待修订账本中上一轮未完成修订；③确认 v4.10.1 是否已
> release。
> ③的答复：**已 release**——tag `v4.10.1` 指向 commit `94bca89`（= 当时 HEAD），
> AGENTS.md「版本号 commit 同步打同名 tag」已被执行，无需补打。
> 范围拍板（用户）：本版 = hero 定性化 + 代码批 7 项 + 测试批 5 项；取数管道 8 项按账本
> 原议继续单独立项。版本号 v4.10.2（patch，参照 v4.8.3 模块化拆分先例）。

## 决策记录

- **判词复用唯一口径**：hero 副标直接调 scoring.py 既有 `_quality_verdict`
  （≥7 好公司/5.5-6.9 中上/4-5.4 一般/<4 回避）与 `_valuation_verdict`
  （≥8 深度安全边际/6-7.9 合理偏便宜/4-5.9 合理无安全边际/<4 贵），不新造判词表；
  公式明细留在 6/7 章（layer-summary 与四件套明细表均在），hero 只答「好不好」。
  时机分 mini 行不动（用户未提，且「微调 ±1 档」是其性质说明不是废话）。
- **`_SCENARIO_NAMES` 下移 scoring**：估值轨归位时 compute_valuation 需要情景语义名，
  但 scoring 是最底层不能 import charts_base（循环）——语义名（悲观/基础/乐观）下移
  scoring，配色留 charts_base，charts_scenario 消费端改按 key 查 `_SCENARIO_COLORS`
  （唯一消费点：spectrum cols 构造）。`_SCENARIO_COLORS` 本身不下移（v4.8.3 十七色
  收编 charts_base 是刻意决策，不动）。
- **重构零漂移为准绳**：代码批/测试批每一步以 golden 逐字节快照 + 全量回归验证；
  有意输出变化共五处：hero 两行判词、.verdict 死 CSS 一行、版本标记一行（快照归一化
  豁免）、try_up 文案归一 1 字节（赔率上浮分支 `）→`→`） →`，热核审计穷举实证后
  认下并申报，见「审计修补」段）；golden 重建前 diff 核验。
- **防伪严格化只收 fill 侧**：`_strict_num`（validate 局部 helper）只用于防伪比对
  （`_check_quote_consistency`），全局 `_num` 宽松语义不动（展示层解析不受影响）；
  「有值但严格解析失败」一律拒渲染（夹带文字即防伪链疑点），ref 落盘侧保持 `_num`
  （脚本生成，本就纯数字）。
- **测试垫片脱钩方向**：render_report 的 noqa: F401 再导出块删除，测试改为从真身模块
  import（_position_steps←scoring、_ticks/_text_w←charts_base、build_segments_plot 等
  ←charts_l1/charts_misc）；其余 R.* 访问面保留（正常 import 面非垫片）。

## 本版变更

### A. Hero 分数定性化（用户新需求）

- `assets/report-template.html`：质量分 s-sub `本质 {{L1_W}}% + 预期 {{L3_W}}% − 黄灯
  {{YELLOW_TOTAL}}` → `{{QUALITY_WORD}}`；估值分 s-sub「四件套加权」→ `{{VALUATION_WORD}}`。
- render_report.py repl 加两键（`_quality_verdict(quality)`/`_valuation_verdict(valuation)`）。
- `L1_W/L3_W/YELLOW_TOTAL` 占位符保留（3/4/5 章 meta 仍在用）。
- golden 重建 diff：恰好两行 s-sub（7.00→「好公司」、5.5→「合理无安全边际」）。

### B. 代码批（账本 7 项全落）

1. **`_preflight` 单流水线**：render() 与 check_fill() 共用（解析→必填→L4 顺序→估值→
   内容校验→评分→估值分可计算），check_fill 缩到壳；实证：缺 company 的 fill 现在
   --check 退出码 2（修复前放行、render 才炸）；叠加审计修补的 `_check_cn_placeholder`
   前置（占位符残留 check 同样拦），fill 侧问题的 check/render 拦截面已对齐
   （渲染后 `_check_leftover` 保留兜底模板/脚本侧残留，属渲染器自检非 fill 问题）。
2. 删 validate.py `_check_peers_orientation` 重复定义（两份逐字节相同，删被遮蔽的第二份）。
3. **魔法字符串常量化**：`_LABEL_REFUSE = "不建议参与"`（scoring，_POS_LABEL/决策链/徽章
   同源；validate 红灯校验改消费常量）；`_C_BLUE` 进 peers 方向校验正则；HTML 字段元组
   三份 → `_HTML_FIELDS`/`_HTML_TABLE_FIELDS`；`_plain_text` 上移 scoring（validate 删
   本地定义，render_report 对齐机三处内联 strip 改调用）。
4. **估值轨归位 scoring.py**：compute_valuation + `_lookup/_lookup_lt` + 四阈值表 +
   `_map_*` + compute_valuation_score 移入；render_report.py 909→573 行。
5. **对齐机独立 align_fix.py**（新文件 256 行）：`_CELL/_TR/_CLASS_ATTR` 常量 +
   _classes_of/_align_class/_set_th_align/_strip_th_align/_is_plain_text/_is_prose_cell/
   _content_vote/fix_table_alignment 搬入；validate 三件套归位（_tag_timing_table→
   align_fix；_check_l4_order/build_prev_strip→render_report——前者 render 单点调用，
   后者 Hero 片段构建与 build_toc 同区）。
6. `_position_steps` 三上浮分支 → `try_up` 闭包。注：归一化顺带统一了赔率分支文案
   （`赔率 ∞（悲观仍正收益）→ 上浮一档` → `） → 上浮一档`，多 1 空格）——热核审计
   穷举 70272 组合发现（1044 组合触发，归一后真差异为零），认下为有意归一，
   test_render_gate 已逐字钉死三上浮路径文案。
7. 顺手小项：render_report:498 ⚠️ 警告补 `file=sys.stderr`（AST 全扫确认其余 ⚠️ 均有
   stderr，CLI 结果输出保留 stdout）；防伪比对严格化（实证：「18.6（H股口径）」现在
   被 `fill.price 不是纯数字` 拒渲染）；RENDERER_VERSION "v4.10"→"v4.10.2"。

### C. 测试批（账本 5 项全落）

1. **golden 快照 1→3**：新增 `full_fill`（10/12 章 + segments/chain/peers_plot/龙卷风/
   triggers/PE 历史带/发丝图/回测哑铃，锚点全在位零锚点告警，83,786 字符）与
   `mcap_fill`（市值口径 + P/NAV 过程卡标签，61,552 字符）；test_golden 泛化
   `_assert_snapshot`，mutation 自证参数化到三份（审计修补）。
2. **pytest 盲区收编**：test_mcap_mode 加 pytest 入口；test_em_fetch 12 区、
   test_calibration 3 区包为 def test_*（原顶层 assert 在 pytest 下零收集、失败表现为
   collection error——「58 绿=全绿」错觉修复）。直跑模式保留（__main__ 逐用例调用）。
3. **test_render_core 拆分**：1272 行 → test_render_gate.py（24）/ test_render_charts.py
   （15）/ test_render_e2e.py（7），46 用例一一对应无删减；新建 conftest.py 收编
   minimal_fill/render_fill/render_workspace/write_fill/expect_valueerror/capture_stderr*/
   validate_stderr/_dim/_calc 十个 helper，19 处行内 import 清零（含 1 份从未被调用的
   死闭包删除）；test_golden 的 minimal_fill 导入改 conftest。
4. **charts_base 收编**：`_hgrid_ticks`（横向网格 3 遍，fs/dy/labels 参数吸收差异）、
   `_legend_row`（矩形图例 4 处）、`_inject_chart_anchors`（锚点注入 2 遍合并）。
   子代理额外做 sha256 逐字节指纹验证（18 个 build_*/_inject_* × 10 用例前后完全相同）。
   判定不同构保持原样：双轴图例（竖线 glyph）、线图例（虚线变体）、圆点/◆ glyph、
   龙卷风极轴网格、散点图直接标注。
5. **死代码四件**：charts_base `_fmt_price`（零调用，def+docstring+re-export 三处清）；
   render_report 测试再导出垫片删除；模板 `.verdict` 死 CSS 删除（golden 有意重建一行）；
   em_fetch re-export 注释补 score_calibration 消费方（`_em_kline_url` 实际被它经
   `em.*` 消费，注释原先只写宿主函数体与 test_em_fetch）。

### 回归状态

- `python -m pytest scripts/ -q` → **78 passed**（58 原 + em_fetch 12 + calibration 3 +
  mcap 1 + golden 新增 2 + 审计新增 gate 2；test_render_core 46 用例拆分后总数不变）。
- 五个直跑文件全过（test_golden 5 / test_em_fetch 12 / test_calibration 3 /
  test_mcap_mode 冒烟 / test_extract_review 7）。
- golden 有意重建两次（hero 判词两行 / .verdict 一行），均 diff 逐字节核验。

### 热核审计修补（修订完成后，thermo-nuclear-code-quality-review 四路并行）

四路审计（render/validate/align_fix；scoring；charts 五文件；测试+文档），均用
`git archive HEAD` 抽出 v4.10.1 源码做双版本对照取证，不依赖自证：

- **P0（已处理）**：try_up 归一化引入 1 字节未申报漂移（赔率上浮分支多空格）——
  70272 组合穷举，1044 触发、归一后真差异为零；三份 golden 均未覆盖该分支故未红。
  处理：认下为有意归一（三上浮分支写法从此一致），test_try_up_wording_nailed 逐字
  钉死；本段即申报。
- **P1-1（已处理）**：fill 含【待填】类中文占位符时 --check 退出 0 而 render 才炸
  （`_check_leftover` 只在渲染后查）——`_check_cn_placeholder` 前置进
  validate_content（_HTML_FIELDS 全字段、a-d 类别豁免同口径），check/render 拦截面
  补齐，双通道实证 exit=2。{{KEY}} 字面量不查（fill 里合法，渲染时实体化）。
- **P1-2（已处理）**：决策链上浮分支零断言零快照覆盖（漂移溜过的根因）——除上条
  逐字断言外，补 test_verdict_word_boundaries（hero 判词分档边界 12 点，hero 副标
  本轮首次直接消费这两个函数）。
- **P2 已修**：render_report._num / validate._esc 死 import 删除；`_SCENARIO_NAMES`
  消费方（validate/charts_scenario）改从 scoring 直导，charts_base 再导出残片删除
  （反垫片精神前后一致）；CHANGELOG「mcap 去 __main__ 守卫」表述纠正（守卫保留，
  仅加入口）；conftest「零告警」不实措辞纠正；fill-schema 的 .verdict 引用改写
  （类已删，规则改述为「禁止灰色小字充当末拍」）；README 快照描述同步三份；
  compute_valuation docstring 键名笔误（price_lo/price_hi → low/high）修正；
  本文件 KB 误标改字符数。
- **P2 记录在案不修**：_check_l4_order 归属 render_report（可辩护判断）；_legend_row
  默认值只服务一个调用方（信息性）；test_mcap_mode 六段共用单个 pytest 入口
  （首失败屏蔽后续，可后续拆）；test_em_fetch 既有零碎（try 前补丁/临时目录不清理/
  覆盖清单滞后/函数号与内部标签错位——非本轮引入，随用随修）。
- **审计确认健康（勿动）**：估值轨/对齐机/三件套搬迁逐字级一致；charts 三 helper
  参数面吸收的是真实差异（非磁铁函数）；_legend_row 转义标签+未转义量宽实现正确；
  锚点注入告警文案 stderr 哈希逐字节相同；46 用例拆分零删减；em_fetch 12 区随机
  顺序 144 次执行零失败；全仓无重名顶层函数。

## 待修订账本结转（下版立项候选）

### 取数管道（8 项，原样结转，账本注明独立大立项）

em_core.py 线性化（总前置：em_fetch↔em_data 双向循环依赖 + 动态转发 stub 单向化）→
em_data 按数据集拆四块 → 进程缓存五套统一 memo(key, loader) → fallback 降级链下沉 →
search_e7 走 get() 统一管线 → `_sec_e6` 静默吞错补 `[失败: ]` 行 → extract_review 未命中
静默加固（代码参数零归一化/miss 与豁免不可区分/listdir 非递归/文件名解析失败零提示）→
em_data 错误政策块六连复制收编 _ts_quiet + 日期窗口 _win_days/_win_years。
**动运行时行为，须单独版本 + 全量回归。**

### 文档批遗留（低优先，随用随修）

- 版本标记考古层（无锚"新增"/"补充修订二四"）统一补版本号；TypedDict 补强 fill 契约
  （YAGNI 评估后仅记录不做）。

### 既有遗留观察（结转状态更新）

- F1 字体本机效果**仍待用户评估**（朱雀仿宋观感；回退预案在 v4.10.0 handoff）。
- 存量 `_em_*.json`/fill 不回溯归档（归档制自 v4.10 起对新报告生效）。
- 报告生产提效（v4.9.1 起结转）：多片段 fill+合并脚本改造继续挂起。
- ~~R.* 再导出耦合~~（v4.9.1 起结转）——**本版已解**：垫片删除 + 测试真身导入。
- em_data.py CRLF 行尾（不紧急，维持原状）。
- 3.6 metric-row 对齐未复现（v4.9.2 起结转）：预案在案，再现再修。

## 教训与记账

- **「58 绿=全绿」是收集盲区的错觉**：三个测试文件游离 pytest 收集外（__main__ 守卫 /
  无 def test_）。回归口径一律以 `python -m pytest scripts/ -q` 总数为准，直跑为补充。
- **遮蔽定义要进 diff 审查清单**：`_check_peers_orientation` 双定义存活一个版本
  （v4.10.0 引入），两个审计员独立命中才发现——同名 def 重复应列入自查 grep 清单。
- golden 三快照覆盖后，builder 级漂移无处可藏；后续加新图字段时同步加进 full_fill。

## 测试与部署状态

- 76 pytest 全绿 + 五直跑全过；渲染行为变化仅限 hero 两行判词与 .verdict CSS 删除
  （其余全程零漂移）。
- 未 commit（按 AGENTS.md 流程待用户确认，确认后同步打 tag v4.10.2）；未部署。
