# v4.11.1 交接 — 预期差逐机构分布图落地 / 季K 趋势均线 / 外部审核两 P1 修复 / 海油六问处置

> 三线触发：① 同日早 session「绘制机构净利预期图表」（e5ab0d3a）定稿的第 8 章机构分布
> 定位图只定形未落地（demo v3 在 artifacts/），海油报告暴露缺口，用户指认后正式立项；
> ② 第三方对 v4.11.0 的全量审核报告发现 2 P1 + 7 P2，本会话复核 9/9 属实（D1/D2/D3 附
> 最小复现），经用户拍板全修；③ 中国海油 600938 报告用户六问，逐项拿产物对勘后处置。
> 版本定位 patch 由用户拍板。回归 96 → 105 项全绿，golden 三快照有意重建 ×1（diff 已核验）。

## gap 图落地（最大件；形态零自行发明，全部溯源上轮 session 拍板）

- **为什么上轮「定好的图」没生效**（用户质询的正式答复）：上轮 10:32 demo 定稿（用户
  「这个差不多了」）后同会话转入天齐四问，膨胀为整个 v4.11.0；12:14 用户拍板 v4.11.0 内容
  只含季K 三件，gap 图以「CHANGELOG 立项 + 口头答复一句」挂起——无任何跟踪载体，导致
  用户以为已就绪。**教训：demo 定形与代码落地之间必须有立项条目（handoff 待办），
  不能只靠口头约定。**
- 落地六步（均按定稿）：E5 增逐机构明细照抄行（`fetch_consensus` 保留 detail/aim_detail，
  同机构 180 天取最新、np 万元→亿、目标价 (min+max)/2）→ `build_gap_plot`（A 档横向分布/
  B 档偏离哑铃，色值/行距 70/轴域 [280,840]/右列 880/防叠三层/◆ 标签阶梯全部按 demo v3）
  → `<!--GAP-->` 锚点注入 gap_html 章首（`_inject_chart_anchors` 增 prepend 形态，第 8 章图
  是章主体不能堆结论框后）→ fill-schema gap 行改写（对照表废止、文字零损失进附注）+
  SKILL 自检 + 模板 `.gap-notes` CSS → `_check_gap_plot`（session 未定校验，本版补设计：
  非法行拒渲染；consensus≤0 剔除/<2 维/同机构多点/目标价与中枢失配 >2% 告警——org 名真实性
  不可机械校验，靠「照抄 E5 明细」条款+附注来源兜底）→ 测试（A/B 双态、防叠确定性两跑
  逐字节一致、注入三路）。
- 实现与 demo 的目检差异（手工摆位 vs 确定性规则，均申报）：定域取整网格复现不了 demo 的
  手工加宽行 1 少右缘刻度、本文/major 标签纵向位统一规则化、行 4 多一个与本文同值刻度
  （规格只省近 ◆ 刻度）、B 档 demo 两处蓝点几何与其标注%自相矛盾按规则统一。
- 契约增补（实现中发现、session 未定）：`reverse:true` per-dim 标 B 档反推口径；
  `note` 直写 HTML 且编号由填写方自持（①②③④ 对行、text_dims 续⑤）；混合档=有 street
  的行画分布、缺的行只画 本文/◆ 两点。

## 季K 趋势均线（海油六问 H6）

- 季K 模式叠加季度收盘 MA4、发丝回退叠加月收盘 MA12（暖灰细线+图例+source 文案）——
  单看蜡烛不易读趋势的反馈直达；均线值域含于价格域，不动定标。

## 外部审核修复（复核 9/9 属实，全修；v4.11.0 handoff 补记有逐项定位）

- **P1 D1**：负利润+非 12 月 horizon → 年化出复数 → TypeError 裸崩（render 只捕 ValueError）。
  修：负中枢不年化（central=central_raw）+ render 捕获面扩 (ValueError, TypeError) +
  validate 负 profit 告警（提示正常化利润/mcap 口径，不拒——困境反转是合法分型）。
- **P1 D2**：三情景跨档倒挂只告警不拒 + 负离散度被误判「低不确定性」上浮一档（实证轻仓→
  标准仓）。修：validate 跨情景中枢顺序硬拒（pess≤base≤opt）+ `_position_steps` 离散度
  上浮加 0 下限守卫（防线二）+ compute_valuation 告警文案指向拒渲染。
- **P2**：D3 `_check_dim_blocks` 废位置 zip 改 dim-name 编号映射（与治理 strip 同口径，
  兜底按位置+每字段一条告警）；D5 quote 缺失日期门禁（≥2026-09-02 拒，存量豁免）；
  D6 E4 A股全空/季度序列 <3 期补降级提示；D7 f-string 内嵌推导式抽变量；D8 dim-block
  切块容忍附加类，三处收敛 `_split_dim_blocks`；D4 data-sources.md 北交所 secid 改 920 段。
- S 级：S2 并入 D1；S4 已做（fill-schema 硬校验清单补齐漏列四条 + 两条新项 + 单一权威源
  声明；不做自动生成脚本——清单语境注释多，机械生成降质）；S1/S3/S5 按原议留下版评估。

## 海油六问处置（H1-H3 文档补强；H4/H5 核实非缺陷）

- H1：scoring.md 税后股息节 + fill-schema 3.5 + SKILL 自检补 A+H/红筹宣派币种口径条款。
- H2：「看量不看价」补适用范围注（仅限增长验证归因语境）+ 周期股结论双覆盖自检行。
- H3：DCF 70%/30% 注明经验阈值 + 正常化中段利润附录逃生口（用户显式要求时）。
- H4：本会话初稿曾误判「图已渲染」（把机构预期图当成 growth_plot），经用户贴示意图纠正——
  实为 demo 未落地，本版实现。教训已录 `tasks/lessons.md`（先锁定用户所指、demo≠实现）。
- H5：同业表 6 方向列 B/W 标注与 CSS 均在位，现价/市值无方向列按 peers_meta 声明豁免，合规。

## 测试与回归

- pytest 105 项全绿（96 → +9：负利润不崩 / 倒挂拒+离散度守卫 / dim-name 映射 / quote 日期
  门禁 / 附加类容忍 / E4 降级提示 / E5 逐机构明细 / gap 图双态防叠 / gap 校验；季K 均线断言
  并入既有季K 用例）。
- golden 三快照有意重建 ×1：价格图趋势均线折线+图例（full_fill）与模板 .gap-notes 4 行 CSS
  （三份）——diff 逐行核验无逻辑漂移。
- 生产验收：海油 600938 报告用 v4.11.1 修订重渲（fill 增 gap_plot 照抄新 E5 明细 + 3.5 补
  宣派币种句 + 结论卡「看量不看价」表述限缩 + gap_html 改锚点结构），产物
  `D:\个股深度分析\中国海油-600938-*-v2.html`（不覆盖原版）。

## 教训

- demo 定形 ≠ 实现：口头「下版立项」+ CHANGELOG 一行不足以跟踪，必须有 handoff 待办条目。
- 「验证过 SVG 存在」只证明某张图渲染了，不证明用户要的那张渲染了——先锁定所指再验证。
- 审核 D2 的同源教训：文档承诺的校验要在代码里找得到，三处清单维护必有单一权威源。

## 热核审计（发布前四路并行，全修）

- **四路分工**：①gap 图实现 ②validate/scoring 修复批 ③取数层与季K 均线 ④文档↔代码
  一致性与版本完整性。无 P0；P1 五项、P2 十余项全部修复或定夺，修复后回归 105 项全绿、
  golden 零漂移（审计④以 GOLDEN_UPDATE 之外方式重渲三份 fill 逐字节比对实证）。
- **P1 修复清单**：major 标签升档后不入册导致互撞 + 本文标签与升档标签跨带相交
  （→ 分带注册表 + 跨带查本文矩形 + 两档皆撞降级仅机构名，点永不删）；B 档 tag 硬编码
  「（反推口径）」（→ 仅确有 reverse 行时输出）；`_check_gap_plot` 对非 dict 的
  gap_plot/street 元素 AttributeError 裸崩（→ isinstance 守卫，与 dims 非法行同标准拒）；
  fill-schema 宣称 thesis_html 空拒渲染而实现为告警（→ 文档更正为告警口径）；
  fill-schema 清单漏列 gap_plot 两条硬拒（→ 补列，并补 pre-existing 漏列项：scores 缺维/
  权重和≠100/valuation_score 越界/dim-block <40 字地板/quote 结构非法与夹带文字）。
- **P2 修复清单**：硬拒前告警湮灭（→ validate_content 拆 impl+包装，raise 前 flush warns）；
  --check 捕获面与 render 不对称（→ check_fill 同步收编 TypeError）；B 档超域夹点视觉撒谎
  （→ 虚线棒+空心蓝点截断记号+source 注明）；附注与图行截断不同步（→ notes 同 [:6]）；
  `_gap_fmt` 大数值科学计数法（→ ≥10000 千分位）；aim_detail 排序键未 float 化
  （字符串价会静默炸掉 tushare 分支→修复）；E5 明细行截断与家数声明矛盾（→ 「前 20/共 N 家」）；
  均线图例/source 与折线门槛分离（→ 同门槛 ≥2 点成线，quarters≥5/n≥13）；
  CHANGELOG ±9±11px 误记（→±9px，demo 注释口径与实现归一）；reverse 仅 B 档生效
  （→schema 注明）；SKILL 目标价告警补双门槛。
- **定夺不修（记录）**：`_gap_axis` 不可达兜底两行保留（防御）；`_gap_ticks` 浮点贴边需
  值/span 比 >1e9 才触发（真实数据不可达）；E4「h 空但季度序列 ≥3」格叙述矛盾需双源
  不一致才可达（近不可能）；conftest `_dim` 无 dim-name 的兜底告警噪音保留（使漂移可见）。
- **审计④实证相符项**：S4 清单补齐项与代码逐条一致；海油 v2 产物在位；v4.11.0 发布四件
  齐全（tag/release/部署核实）；版本号无残留漂移。

## 部署状态

- 已全部完成：commit 30993c7 → push main → tag v4.11.1（已推远端）→ gh release v4.11.1
  （Latest）→ deploy.ps1 -Go 部署生效（部署目录已恢复只读）。后续两个纯流程小 commit：
  deploy.ps1 排除清单补 tasks/（3679363）+ 补回 BOM（1c2890d）。
