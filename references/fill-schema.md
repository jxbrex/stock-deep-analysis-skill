# fill-data JSON 契约（render_report.py 输入规范）

渲染流程：模型把全部分析内容写成一个 JSON 文件 → `python render_report.py fill.json`
→ 脚本完成评分计算 + 占位符替换 + 校验 + 自动命名输出。**模型不需要碰 HTML 文件本身。**

**数据直填第一原则（防幻觉总纲）**：凡有 em_fetch 出处的数字——`quote`/`valuation_inputs`
（E1 落盘）、`timing` 技术面信号、`pe_history` 的 p25/p75、`price_history` 月末 PE、
`segments`（E6）、`holders`（E4）、`consensus`（E5）——**一律照抄 em_fetch 输出或落盘
JSON 原样回填，禁止手估、手算、凭记忆补**。各字段的直填来源见顶层字段表对应行。

## JSON 书写硬规则（违反即解析失败）

- **禁止裸反斜杠**：fragment 里任何 `\` 在 JSON 字符串中必须写成 `\\`（`\ `、`\P` 等都是非法转义）。
  表头、分隔符一律用全角 `＼` 或 `/`，从源头避开反斜杠（见 9 矩阵示例）。
- 字符串内换行用 `\n`，不要直接回车；双引号用 `\"` 或改用中文引号「」。
- **字符串一律双引号，禁止 Python 单引号**——分段 append 时尤其容易把字段写成
  `'key': 'value'`（Python 字典习惯），json.load 直接失败。
- fragment 内嵌 HTML 的属性引号：`class="..."` 需 `\"` 转义，写单引号 `class='...'` 也可以——
  渲染器入库时自动把 class 单引号归一为双引号（v4.4 起自动归一），
  不影响校验与输出；报错/告警文案里一律以双引号显示。
- **【…】 只允许黄灯类别标注**（如【b 行业与政策环境】，以单个 a-d 字母开头）；
  其他用途一律用（）或「」——其余【…】会被渲染器判为未替换占位符而拒渲染。
- 修补已写盘的 fill：**先 Read 再 Edit**（脚本/外部过程改过的文件直接 Edit 会被
  staleness 守卫拦截；报 `old_string not found` 就是你跳过了 Read——先 Read 再重发 Edit）；
  分段写盘时按字段边界切段。**Edit 锚点选不含 `\"` 转义的纯文本片段**（单行长 JSON 里
  锚点跨转义引号极易失配；也不要从 python repr 输出反推原文，repr 会把反斜杠加倍）。
  大段返工不要逐点 Edit——用 Write 重写该字段更快更稳。
- 渲染成功后 fill 与 E1 落盘由 render 自动归档到报告目录 `_archive/`（v4.10 起，不再是删除）——
  修补已归档的旧 fill 请从 `_archive/` 取回原件再改。
- 写完后的自检**只有一条合法命令**：`python "<技能目录>/scripts/render_report.py" "_fill_[代码].json" --check`
  （JSON 解析 + 评分计算 + 内容地板全预检，退出码 0 才能渲染）。**禁止手写 `python -c` 内联自检**
  （Windows 控制台引号/编码坑；本环境 shell 是 Git Bash，路径一律用正斜杠，
  `D:\...` 反斜杠路径在 bash 下不存在）。
- render_report.py 已带非法转义自动修复（兜底+告警），但不保证覆盖所有写法，仍以写对为准。

## 顶层字段

| 字段 | 必填 | 说明 |
|------|:----:|------|
| `company` | ✓ | 公司名（如 "宝丰能源"） |
| `code` | ✓ | 代码（如 "600989"；港股 5 位数字如 "06082"） |
| `currency` | 可选 | 股价/目标价的币种单位（默认 "元"；港股填 "港元"，其他币种同理）。模板单位与情景表目标价行均由脚本统一替换 |
| `date` | ✓ | 报告日期，必须严格 YYYY-MM-DD 格式（不匹配即拒渲染） |
| `subtitle` | ✓ | 副标题（行业标签 · 一句话定位）。**禁止包含报告日期**——模板 Hero 自动追加「｜报告日期：date」，手写会渲染出两次（渲染器会防御性剥离并告警，仍以写对为准） |
| `scores` | ✓ | **质量层** 9 维得分对象：`{"1A":8.0,"1B":7.0,"1C":8.0,"1D":8.0,"1E":7.0,"1F":7.5,"3A":7.5,"3B":8.0,"3C":7.0}`（v4.0：L1 六维 1A-1F + L3 三维 3A-3C；2A 估值独立成估值分） |
| `valuation_score` | 无需填写 | **估值分永远由脚本计算**（四件套，见 `valuation_inputs`），fill 里不再需要该字段——填了也被脚本计算值覆盖（差值 >0.11 时告警提示删除） |
| `valuation_inputs` | ✓ | **估值分四件套输入（必填，缺键拒渲染）**：`{"pe_ttm":13.5,"pe_band":[14.5,15.5],"div_yield":1.6,"risk_free":1.7}`（pe_ttm 现价 PE、pe_band 合理带 [低,高]、div_yield 税后股息率 %、risk_free 无风险利率 %）。脚本据此自动算估值分，并在 7 估值章末尾汇总卡生成四件套明细（6 章只汇总质量分，不重复估值/时机明细）。**可选 `metric_label`**（如 "P/NAV"、"P/rNPV"、"P/EV"、"经调整PE"）：行业附录市值口径（见 `scoring.md`「市值口径三情景」节）下替换过程卡 PE(TTM) 标签，pe_ttm/pe_band 数值语义随之变为对应比率，映射公式不变 |
| `weights` | 可选 | 质量层维度权重覆盖（L1 六维层内权重之和=100；L3 三维层内权重之和=100。分型调整时用） |
| `layer_share` | 可选 | 层占比覆盖：`{"L1":85,"L3":15}`（两值之和=100。分型调整时用） |
| `timing_scores` | ✓ | **时机层**得分对象：`{"筹码面":2.5,"技术面":6.0}`（旧键名 2B/2C 兼容已移除，误用直接拒渲染）。两维各自**中性 5 分起始 + 清单信号加减**（评分锚见 `scoring.md` 时机分一节），禁止凭感觉给分；**缺键或缺维度即拒渲染（筹码面/技术面缺一不可，不接受缺维按 0 计）** |
| `timing_weights` | 可选 | 时机层权重覆盖，默认 `{"筹码面":67,"技术面":33}`，和必须=100 |
| `yellow_deductions` | ✓ | **黄灯扣分明细**数组：`[{"label":"关联交易占比过高","points":0.6}, ...]`；无扣分填 `[]`。**硬校验：键缺失即拒渲染（无扣分必须显式填 `[]`）；单项 >1、累计 >2 或 points 为负 → 拒渲染**（累计超上限按规则应升红灯；负扣分等于变相加分、绕过上限）；质量分扣完后下限 0 |
| `red_deductions` | 可选 | **1D 红旗扣分明细**数组：`[{"item":"现金含量连续2年<0.7","points":1}, ...]`；**每项 points 上限 1（超出拒渲染）；points 为负同样拒渲染**。脚本从 `scores["1D"]` 中减去合计（下限 0），并在 6 质量分汇总表中单列一行展示 |
| `red_flag` | 条件 | **红灯标记**：命中红灯填具体项（如 "审计意见非标：无法表示意见"），未命中省略或填 `""`。**熔断硬校验：red_flag 非空时 `position_html` 必须包含「不建议参与」，否则拒渲染** |
| `thesis_html` | ✓ | Hero 一句话结论（**为空即拒渲染**），**全文提纲挈领句 = 论点 + 关键证据 + 三情景价 + 操作结论**（含 `<span class="scenario-pess/base/opt">` 三情景价）。正例：「全球玻纤龙头+AI电子布第二曲线，中报净利+73.9%兑现高景气，但股东户数单季暴增254.6%且PE处5年90%分位——好公司、坏价格未变。三情景目标价 15.7/37.5/61.8 元，年化中枢 -10.9%——回避当前价，等 25-29 元观察区」。**只塞三个目标价 = 不合格**（纯文本 <60 字或剥掉三情景价后 <20 字 → 告警）。**一致性硬校验：三个手写价与脚本按 `valuation` 算出的情景中枢价对比，相对偏差 >2% 且绝对差 >0.1 → 拒渲染**（报出两组数值）；span 类名不齐仍是告警 |
| `price` / `mcap` / `pe_ttm` | ✓ | Hero 指标卡数值（纯数字字符串）；**price 缺失或无法解析为数字即拒渲染** |
| `quote` | 建议 | **E1 落盘防伪（神华 601088 现价造假事故修复）**：`{"source_file":"_em_600989_quote.json","date":"2026-08-28"}`——source_file 指向 `em_fetch --out` 落盘的 E1 JSON；渲染器读取该文件并比对 `price`/`pe_ttm`，**偏差 >1% 拒渲染**（与估值分四件套同级），文件读不到同样拒渲染（防伪链断裂不放行）。**v4.8 扩展到四件套**：`valuation_inputs.pe_ttm` 与落盘 pe_ttm 偏差 >1% 拒；`pe_band` 与落盘历史带完全无交集拒（部分重叠正常）；`risk_free`/`div_yield` 与落盘值偏差 >0.3pct 告警不拒（手工估算/税后折算口径须在 .source 注明；港股 div_yield 不机械比对）。行业口径（`metric_label` 非空）下 pe 两项比对跳过。字段缺失 → 告警不拒（存量 fill 兼容），新报告必配。落盘 JSON 另含 `timing`（现价/MA60/MA120/52 周高低，日线序列计算）——**11 章时机判定小表的技术面信号一律取自该字段，禁止手估**（神华假 MA60 同源修复） |
| `price_sub_html` / `mcap_sub` / `pe_sub` | ✓ | 指标卡 sub 行（可含 `<span class="up/down">`） |
| `horizon` | ✓ | 目标价时间维度（如 "12个月"） |
| `target_range` / `target_sub_html` | ✓ | 目标价区间卡。**target_range = 基础情景目标价区间**（如 "86-93"），脚本按 `valuation` 自算，不一致按计算值覆盖并告警——**不是三情景全区间**（误填成悲观~乐观全区间会被覆盖）；target_sub_html 写该区间对应现价的空间说明 |
| `conclusion_html` | ✓ | 1 核心结论正文。**四卡固定**（v4.9.1 补充修订二：`.concl-grid` 2×2 结论卡网格，顺序 row-major ①②/③④）：①关键优势（恰 3 条壁垒短语+括注数字锚）→ ②关键弱点（2-3 条不回避的弱点+括注数据）→ ③当前市场认知（恰 2 条：卖方假设带来源日期 / 定价隐含假设，与 8 呼应）→ ④核心投资逻辑（恰 3 条固定槽位：论点 / 可观测证伪条件 / 路径判断=未来 1-3 个月市场最可能先交易哪个变量）。**四卡统一 `<ul>` 短列表**（补充修订四：每条 ≤12 字短语 + 至多一个全角括注 ≤15 字（数字/日期/来源），括注不加类；不带展开论述——论证由被引章节承载），每卡=`.concl-head` 卡头 + `<ul>` 短列表 + `.concl-ref` 卡尾「详见 X 章」锚索引（固定映射：优势→`#s3`／弱点→`#s5`（财务弱点可并引 3.4）／市场认知→`#s8`／投资逻辑→`#s7`·`#s11`；只链无条件章节，不链 s10/s12）；浓缩省略的信息由被引章节承载，不在结论章重复。**禁止 `.metric-row`/评分表/判词卡**——评分数字在 Hero 与 section-meta 已呈现，正文只写文字。命中红灯 → 首部 `.danger-card`（脚本 `RED_FLAG_HTML` 自动注入）；红旗命中≥2 项 → 首卡首句"利润真实性存疑"；显著情绪/筹码风险 → 末尾 `.warning-card`。**内容地板：纯文本 <120 字拒渲染**（补充修订四下调——四卡 ul 短列表化后原 200 字地板会误伤合规卡）。**骨架见下方「核心结论四卡骨架」** |
| `p0_html` | ✓ | P0 关键利润驱动：`<span class="section-tag">分型：XX股</span>`（**独占一行**——标签后必须另起 `<p>`，禁止识别特征直接跟在 span 后；渲染层 `.section-tag` 已是 block，写错也会被强制换行）→ 识别特征一段（分型理由/层占比/估值方法）→ 引出关键变量一句 → 敏感性由顶层 `sensitivity` 字段承载（脚本生成龙卷风图，含变动幅度/净利影响%/金额影响；写了字段就不要再手写敏感性表——图替代表）→ `.info-card`「为什么 X 是第一变量」（解释杠杆最大原因 + 三情景如何围绕它定义）。参照腾讯控股 2026-08-08 报告 P0。**变量命名规范（v4.9，青啤「①吨价/产品结构（第一变量）」难读实证）**：变量名只放一个概念——驱动因素写正文不进名字（错：吨价/产品结构 → 对：吨价（啤酒平均售价），「靠高端化拉动」写进解释句）；行业黑话首次出现必须白话括注（吨价/批价/量价齐升等）；「（第一变量）」是龙卷风图排序标签，正文枚举不再重复贴 |
| `l1_html` … `l3_html` | ✓ | 质量层评分正文片段：**写作细则见下方「l1_html / l3_html 写作细则」**（v4.9.2 自本行整行拆出，内容不变） |
| `l4_html` | ✓ | **L4 风险双层**正文片段（**红灯 hero 三卡**（v4.9.1 补充修订二，章首固定，骨架见下方「红灯 hero 卡骨架」）+ **黄灯扣分明细表**（v4.10 起只列 points>0 的类别行，无扣分类别不入表、表末一句兜底，骨架见下方「L4 黄灯扣分明细」）+ **损失预演三联卡**（v4.10 起固定骨架，见下方「损失预演三联卡骨架」；标题固定写「损失预演」，禁止用英文 Pre-mortem）） |
| `valuation_method` / `stock_type` | ✓ | 7 卡片头（如 "PE历史时段匹配法" / "周期股"） |
| `valuation` | ✓ | **结构化三情景假设（必填，不完整拒渲染）**——脚本算出目标价区间/EPS/较现价/中枢/赔率/离散度，并自动生成 7 情景表+三指标卡+走廊图，target_range 不一致会被计算值覆盖：`{"shares":46.27,"horizon":"12个月","scenarios":[{"key":"pess","label":"悲观","trigger":"价格战重启","profit":850,"pe":[16,18]},{"key":"base","label":"基础","trigger":"出货+30%","profit":930,"pe":[22,25]},{"key":"opt","label":"乐观","trigger":"储能超级周期","profit":1000,"pe":[26,30]}]}`（shares 亿股、profit 归母净利亿元、pe 区间）。**必须含 pess/base/opt 三情景；每情景需 profit（净利）+ pe 区间 + horizon（可放情景级或 valuation 级）**。**口径二选一**：行业附录市值口径（NAV/rNPV/SOTP，见 `scoring.md`「市值口径三情景」节）每情景用 `"mcap":[低,高]`（目标总市值，亿元）替代 profit+pe——目标价 = mcap÷shares 脚本换算，情景表自动改显示「目标市值」行；三情景必须同口径、同情景不得双填，混用/双填/mcap 区间非法一律拒渲染；**shares ≤0、利润口径 PE 区间倒挂（高<低）或为负同样拒渲染**。**每情景 `trigger` 写 ≤15 字短语**（v4.10 起三情景表触发条件行随列右对齐，长句换行难看——工行「NIM 降至 1.22%、信用成本升至 0.65%」24 字实证）；情景表/目标价区间卡/走廊图的目标价由脚本统一展示（v4.10 起 <10 元两位小数、≥10 元整数，低价股区间不再被整数格式化压没） |
| `scenarios` | 条件 | **valuation 的简化兜底**（只给目标价区间，脚本只生成走廊图，表格/指标卡仍需手写）：`[{"key":"pess","label":"悲观","low":294,"high":331}, ...]`；**填了 valuation 就不用填它**（valuation 已为必填，本字段仅为兼容保留） |
| `valuation_html` | ✓ | 7 正文（校准逻辑+方法说明；三情景表/三指标卡/四件套过程卡均由脚本生成，**不要手写**）。**PE 校准 info-card 只写结论**：时段选取逻辑一句带过 + 注明「完整时段拆解与历史带图见第 10 章」——相似时段 PE 对照的完整推导住第 10 章（v4.7.1 分工，禁止两处重复展开）。周期股分型时建议把校准 info-card 写成本字段最后一块 |
| `gap_tier` / `gap_html` | ✓ | 8 预期差（档位 A/B/C + 正文；卖方假设必须带来源；**末尾一句话总结**：比卖方更乐观/悲观/一致 + 证伪时修正方向）。档位含义须在正文首句点明：A=找到具体卖方假设（有出处，完整对照表）/ B=只有一致目标价与评级（反推隐含假设）/ C=什么都找不到（只列分歧维度，标"未披露"）。**对照表**：首列=卖方假设/维度（内容不换行），单元格不加类、对齐由渲染器自动完成（见「表格」节） |
| `peers_meta` / `peers_html` | ✓ | 9 同业对比（当前指标表+趋势表+`.conclusion-box` 3-5 条结论；**公司强制为行标题**（v4.9.1 补充修订二：两表统一，首列公司名 + `freeze-first` 冻结，指标为列；目标公司**行首格** `style="color:#4a6fa5;font-weight:700;"`——蓝 style 落在 `<th>` 表头=误写成公司列，渲染器告警））。**内容地板：不含 `<table>` → 拒渲染** |
| `peers_plot` | ✓ | **估值-质量散点图数据**（脚本生成 9 顶部 SVG 散点，替代 3×3 表格矩阵；**缺失=图不生成+渲染器警告**，仅同业数据实在凑不齐时才允许省略并手写 matrix-table 兜底）：`{"points":[{"name":"宁德时代","roe":24.7,"pe":21.3,"target":true},{"name":"比亚迪","roe":15.1,"pe":29.8}],"pe_bands":[15,25],"roe_bands":[8,15]}`；bands 可省（默认 PE 15/25、ROE 8/15），直接给数组亦可；points 可选 `"size"`（总市值，点大小 ∝ 市值 sqrt 缩放）；**目标公司 pe 必须与 `valuation_inputs.pe_ttm` 同口径**（A/H 股、IFRS/经调整——偏差 >30% 渲染器告警，确需混排在 `peers_meta` 注明） |
| `sensitivity` | 可选 | **敏感性龙卷风数据**（脚本生成 2 章末双向弹性图，**替代 P0 手写敏感性表**——写了本字段，P0 正文就不要再写敏感性表；缺失=图不生成）：`[{"name":"矿产金售价（金价）","impact":20,"delta":"±10%","amount":"净利约±9-10亿元"}, ...]`（impact=该变量变动对归母净利的影响幅度绝对值 %；delta/amount 可选，填了即在变量名下展示「变动幅度 → 金额影响」子行；脚本按 impact 降序，首行即第一变量）。**name 命名规范（v4.9）**：只放一个概念、自解释（≤12 字），行业黑话带白话括注（如「吨价（啤酒平均售价）」），与 P0 正文枚举同名同口径 |
| `pe_history` | 可选（分型条件） | **PE 历史带数据**（脚本生成**第 10 章**章首历史带子弹图，垫在手写时段拆解前；缺失=图不生成；**cycle_html 缺失 → 整章删除、图无处显示，渲染器告警**）：`{"hist_lo":13.7,"hist_hi":83.2,"label":"近5年","milestones":[{"label":"2021H1","pe":46.9},…]}`（label 可省；hist_lo/hist_hi=历史 PE(TTM) 极值，**须与 `valuation_inputs` 同口径**；行业附录口径下随 `metric_label` 换标签）。**milestones 可选**：3-6 个关键时点（峰值/谷值/典型时段），渲染为带内灰虚刻+时点 PE 标注（v4.8.1 起双层交错排布，近距离时点不再互叠），与时段拆解表同源取数（E2 月线）。**p25/p75 可选**（v4.8）：`"p25":15.1,"p75":20.2`——历史带内渲染深沙色分位区，取数直接用 em_fetch E1 落盘的 `pe_p25`/`pe_p75`，禁止手估。**显示域自适应截断（v4.8.1）**：hist_hi 远超决策值集合（合理带/当前值/关键时点）×1.5 时（如 TTM 净利极薄年份 PE 冲高），图域截到该倍数、右缘标注「峰值→」，填写时仍给全量 hist_hi，不要为了好看缩小 |**填写条件（v4.7.1 分型规则）**：周期股/稳健成长股/稳定价值股/困境反转股——第 10 章必写，本字段按数据可得性填写；快速成长股/未盈利股——**禁填**（历史区间锚不住合理带），第 10 章时段表仍可写（描述性） |
| `price_history` | 可选（分型条件） | **股价/PE 历史发丝图**（v4.8，第 10 章：PE 历史带之后、手写时段拆解之前；与 pe_history 同绑定——`cycle_html` 缺失整章删除、图无处显示会告警）：`{"label":"近5年","series":[{"m":"2021-09","close":12.3,"pe":15.2},…]}`（旧→新月频；**v4.8.1 起 E2 月线输出直接带月末 PE(TTM)（`月收盘/月末PE` 序列），pe 字段照抄即可，禁止再手工按「月收×总股本÷TTM净利」推算**；有效点 <12 不生成；pe 可逐点缺省，过半缺失则只画股价线。分型规则同 pe_history：快速成长/未盈利禁填） |
| `segments` | 可选 | **业务构成图**（v4.8，脚本生成；v4.8.2 起为横条图：横条=收入占比（0-100% 定域）+ 钢蓝圆点=毛利率（同一 % 轴）+ 右列毛利率数值，左列分部名超长自动折行、高度随行数自适应，挂 3.1 赛道与宏观——锚点 `<!--SEGMENTS-->` 写在 l1_html 的 3.1 维度块内（判词前），锚点缺失则图追加第 3 章末尾并告警；字段未填锚点静默清除）：`{"period":"2026中报","by":"按产品","note":"可选·口径清洗说明","items":[{"name":"烯烃产品","revenue":241.1,"rev_pct":79.8,"gross_margin":45.9,"gross_profit":110.6,"gp_pct":84.1},…]}`（金额单位=亿元；rev_pct/gross_margin/gp_pct 为百分数；**name+rev_pct 必填，其余按数据可得性**。数据来自 em_fetch E6 摘要行原样回填，禁手估；占比和偏离 100% 超 5pct 告警——漏列「其他」项要补。**利润口径=毛利**：分部净利润无公开披露（tushare/东财/妙想均只到毛利），禁止编造分部净利。**v4.9：period ≤8 字、只放时段标签**——口径/清洗说明（如剔除合计行重算占比）一律写 note（渲染为图注小字）；period 超长或含「口径/源为/合计行/剔除/重算」字样渲染器告警（青啤实证：70 字说明塞进 period 撑爆图标题）。另：脚本图存在时，3.1 正文不得再写「业务构成（…）」开头的引言句——解读句直接写结论，图标题由脚本出） |
| `industry_chain` | 可选 | **产业链位置图**（v4.8，脚本生成三栏流向图，挂 3.2 产业链位置——锚点 `<!--CHAIN-->` 用法同上）：`{"upstream":["煤炭开采","电力"],"self_note":"煤制烯烃一体化","downstream":["聚烯烃加工","包装","家电"]}`（**只列行业不列企业**——上下游玩家众多，列企业反而以偏概全；各 1-6 个、各至少 1 个，缺一端图不生成；self_note=一句话产业定位，可省。无 API 数据源，来自年报「经营情况」/妙想 `mx_finance_search_notice`/10jqka operate.html 定性检索，禁止编造。**v4.9 起两栏各自在总高内均分**，上下游条数不同则间距不同、上下对齐） |
| `fin_trend` | ✓ | **财务健康五年组合图墙**（v4.9，脚本生成两列双轴组合图面板，**替代 3.4 手写年表**——年表禁写；锚点 `<!--FIN_TREND-->` 写 l1_html 的 3.4 维度块内）：`{"years":["2021",…,"2025"],"panels":[{"title":"营收 × 毛利率","bars":[{"name":"营收","unit":"亿","values":[301.7,…]}],"lines":[{"name":"毛利率","pct":true,"values":[36.7,…]}]},…]}`（bars/lines[].values 与 years 等长、全为数字；每面板=0-2 柱+0-2 线、柱线至少其一（v4.9.1 起支持无柱纯线面板，只出右轴）；**有效面板 <3 或年数 <3 拒渲染**。数据照抄 em_fetch E3 年表，禁手估；行业分型换面板先查对应附录。柱色规则/标准 4 面板/行业专项指标口径等细则见下方「fin_trend 图墙细则」节（v4.10.1 自本行拆出）） |
| `growth_plot` | ✓（未盈利/管线分型豁免） | **利润增长图**（v4.9，脚本生成，挂 4.1 利润增长——锚点 `<!--GROWTH-->` 写 l3_html 的 4.1 维度块首）：`{"hist":[{"y":"2023","rev":5.3,"np":15.0},…近3-5年],"fcst":[{"y":"2026E","np_lo":2,"np_hi":6,"np_consensus":4.7,"rev":3.0},…未来1-2年]}`（增速均为百分数；hist 取 E3 年表同比（rev 可省）；np_lo/np_hi=本文净利增速区间（与 valuation 三情景口径一致）、**np_consensus=卖方一致预期（E5 研报汇总，必填——图的核心是预期差）**、fcst.rev 可省。hist 有效年 <3 或 fcst 无有效年 → 拒渲染。图上自动标预期差（本文中枢−一致预期），8 章预期差对照表保留、不重复展开。stock_type 含「未盈利/管线」时本字段豁免） |
| `holders` | 可选 | **股东户数趋势图**（v4.8，第 11 章；**v4.9.1 起移至章首**（时机判定小表前）——阅读流=证据（户数趋势）→判定（时机小表）→结论（三轨判定卡），与第 10 章 PE 历史带垫章首先例一致）：`[{"date":"2025-03-31","num":188153,"chg":5.2},…]`（旧→新，chg=环比% 可省；数据直接回填 em_fetch E4，≥3 期才生成；环比着色按筹码语义：户数增=分散=红，户数减=集中=绿。港股 E4 不支持则不填） |
| `consensus` | 可选 | **卖方一致目标价带**（v4.8，叠加在第 7 章目标价走廊上的灰色区间带）：`{"lo":294,"hi":331}`——E5 目标价区间（tushare report_rc 聚合或东财 DEC_AIMPRICEMIN/MAX）直接回填；缺失不画。「卖方怎么看 vs 我们怎么算」一眼对照，与第 8 章预期差呼应 |
| `triggers` | 可选（**回测模式必填**） | **触发条件状态条**（v4.9，脚本生成，垫 13 章手写仪表盘前）：`[{"cond":"提价兑现","metric":"26H2 毛利率","target":"≥46%","status":"hit"},…]`（cond 必填；metric/target 可省；**status 三值：hit 已兑现 / miss 未兑现 / pending 待验证**——首版报告全 pending；**回测模式必填，承载旧触发条件核对结果：target 写「阈值｜实际值」**（如 `"target":"≥46%｜实际 47.2%"`）——v4.9.1 起取代 dash_html 手写旧触发条件核对表（双写冗余，药明康德 2026-09 复盘实证），prev 已填而 triggers 缺失渲染器告警；3-6 条为宜，>8 条告警；非法 status 按 pending 渲染并告警。评价语义色：绿=已兑现/红=未兑现/灰=待验证，与涨跌红绿无关） |
| `cycle_html` | 条件 | 10 周期规律（**PE 历史带图垫章首**（脚本按 `pe_history` 生成，概览→明细）→ 阶段拆解表 + `.conclusion-box` 可复用规律）；**空字符串或省略 → 整张卡片自动删除（连同期首历史带图）**。**填写条件（v4.7.1 分型规则）**：周期股/稳健成长股/稳定价值股/困境反转股**必写**；快速成长股/未盈利股可省。**形态按分型**：周期股=商品/产能周期阶段拆解（商品价格/产能/库存）；其余分型=估值周期时段拆解（阶段｜时间｜股价｜PE｜驱动因素，中国移动 2026-08-05 报告即此形态：利润波动 ±5% 但 PE 波动 100%，触发估值周期分析）。时段拆解表与 `pe_history` 同源取数（E2 月线），7 章 PE 校准 info-card 只写结论并引用本章 |
| `cycle_meta` | 条件 | 10 触发条件说明 |
| `next_review` / `dash_html` | ✓ | 13 跟踪仪表盘（**跟踪与触发合并单表**（v4.8.1）：指标 ｜ 当前值 ｜ 触发阈值 ｜ 触发后操作 ｜ 数据来源 ｜ 更新频率，不再分列跟踪指标表+触发条件表两张表；**表必须 `<div class="table-scroll"><table class="dash-table freeze-first">`**（v4.9.1，样式规则见「表格」节；补充修订四起 ≤768px 渲染层自动行转卡片、列名标签由 td::before 生成，免横滚）；**回测模式不再手写旧触发条件核对表**——v4.9.1 起废止，核对结果由 `triggers` 状态条承载（见 triggers 行；prev 已填而 triggers 缺失渲染器告警）；末尾复盘占位提示指向 SKILL.md 6.5/6.6） |
| `position_html` | ✓ | 11 仓位与时机决策正文，**模型只写三块**：①「时机判定小表」（得分口径：中性 5 分起始 + 按 `scoring.md` 时机分评分锚逐条信号加减；**表格固定 4 行 3 列**：维度 ｜ 得分 ｜ 命中信号与加减——筹码面行/技术面行/时机分合计行（= 筹码面×67% + 技术面×33%，标注"微调 ±1 档"）；每格只写短语（≤15 字/条为写作口径，渲染器实际告警阈值为 >40 字；"未命中""港股不可得（跳过）"也是短语），长解释与降级说明一律挪到表下 `.source` 行，禁止在单元格里写整句——长句塞格会导致表格溢出横向滚动；无需手动加类，渲染器自动补 `timing-table`）→ ② 决策逻辑 `.info-card`（为什么是这档仓位，与质量/估值/时机如何联动）→ ③ 触发条件（加仓/减仓/回避的可观测触发条件）。**禁止手写「三轨评分判定表」「三轨仓位映射表」**——三轨判定卡（三轨判词 + 调节轨迹 + 最终仓位结论徽章）由脚本生成并置于本字段**之后**（章末），完整决策矩阵规则见 references/scoring.md，报告只显示落位与结论；最终仓位档位以脚本卡片为准。跨股对比（条件触发）附加在末尾。**内容地板：纯文本 <100 字拒渲染；red_flag 非空时必须含「不建议参与」** |
| `gen_time` | 可选 | 默认用 date |
| `calib_note` | 可选 | 免责声明后缀（如 "PE估值经历史时段匹配法回测校准"） |
| `prev` | 回测必填 | **回测模式上版锚点**：`{"date":"2026-08-08","quality":7.87,"valuation":6.5,"timing":5.50,"target_range":"396-832"}`；填入即进入回测模式（文件名自动加"复盘"，Hero 自动生成对比条，差值脚本计算）。**回测模式下正文凡出现「较上版/新增/证伪」类表述必须挂 `.rev` 高亮**（全文 <3 处会触发渲染器告警） |
| `review_html` | 回测必填 | **R 回测复盘章节**正文（复盘四格表 + 新财报关键数据 vs 原假设对比表）；prev 与 review_html 必须成对出现，缺一则渲染器告警。**禁止另存独立 `_复盘.md`**——复盘内容只进 R 章节 |

### fin_trend 图墙细则（v4.10.1 自字段表行拆出）

- **柱色只表系列归属：沙=第一系列/钢蓝=第二系列**——v4.9.1 起取消单柱「最新年钢蓝」高亮，
  最新年信息由头行最新值+同比与 x 轴最右承载，单双柱口径统一（药明康德 2026-09 复盘：
  钢蓝一身三职致混淆实证）；线=比率·右轴，首条钢蓝实线、第二条灰虚线；pct=true 按 % 展示；
  lines[].threshold 可选红虚线阈值。
- **标准 4 面板（v4.9.1）**：营收×毛利率 / 归母净利+扣非净利×净利率×ROE（扣非柱照抄 E3
  年表「扣非净利亿」列）/ 经营现金流+自由现金流×现金含量（阈值 0.7；**自由现金流=经营
  现金流−资本开支**，E3 年表下方「资本开支」行口径）/ 应收+存货周转天数（**纯双线图**，
  单位「天」，照抄 E3 年表下方「周转天数」行）——可按数据可得性裁剪或换面板（货币资金×
  短债覆盖率（右轴为倍数；>10x 时线趋平，读作「短债无忧」）为常用替换面板）。
- **行业分型（金融/互联网/医药/地产）换面板前先查对应附录的「fin_trend 推荐面板」节**
  （v4.10 新增，含面板组合与数据路径）；行业专项指标（净息差/拨备覆盖率/CET1/合同负债等）
  不在 E3 管道，可来自妙想 F9/年报手查，须在面板下方 .source 注明出处——E3 照抄原则对
  行业专项指标放开此唯一口子（工行 2026-09-09 报告净息差×不良、拨备×CET1 面板实证为优秀
  示范）。现金含量=经营现金流净额÷归母净利（口径见 scoring.md 1D）。

### l1_html / l3_html 写作细则（v4.9.2 自上方字段表整行拆出，内容不变）

质量层评分正文片段：`l1_html` 含六维（**报告内 `dim-name` 显示编号用 3.1-3.6**：3.1 赛道与宏观/3.2 产业链位置/3.3 商业模式与护城河/3.4 财务健康/3.5 治理与资本配置/3.6 资本回报质量；3.4 含红旗四项三态表（现金含量/应收/存货/毛利率；审计意见已挪 L4 红灯 a）+**fin_trend 财务趋势图墙**（v4.9 起必填字段、脚本生成，**禁止再手写 5 年年表**——图替代表；锚点 `<!--FIN_TREND-->` 写 3.4 块内）+**em_fetch 财报可信度初评等级与依据**；

**3.3 商业模式与护城河用清单列示**（补充修订四：复用同一 `.trig-strip` 类族，2-4 条护城河——cond=护城河名称、mt=一句论据（数据+判语，多数据点用分号）、status=护城河评级三态：hit 绿=宽阔 / pending 灰=中等 / miss 红=狭窄（评价语义，与涨跌红绿无关）；每块=单个 `.trig`、禁另起 `<p>` 正文、整块 `<p>` 恰为 2=判词段+评分末拍段（含依据链）、mt 内 `<strong>` 至多 1 处——规则与 3.5 全同）；**论据标准（v4.10.1，判据全文唯一权威在 scoring.md 1C，此处不复述）**：mt 按论据类型满足对应判据——定价权类带分域可观测判据至少 1 条（禁跨域套用）；边际改善类（毛利率回升/不良下降/份额提升）带同步性检验 α/β 结论一句；周期行业毛利率论据须先归因三方向。评分末拍段在依据链后强制**竞争压力测试槽位**：`压力测试：{最大威胁场景} → 扛得住/警惕/扛不住（{5-10 年先例一句}）；侵蚀观测：{信号} {阈值}，已挂 13 章`——给了阈值的 13 章 triggers/dash 表必须有对应行，否则删阈值句；

**3.5 治理与资本配置用分块列示**（v4.9.1：复用 13 章 triggers 同款 `.trig-strip`/`.trig`/`.trig-dot`/`.trig-cond`/`.trig-mt`/`.trig-status` 类族，3-5 块——股权与控制权/管理层与激励/分红与回购/资本运作记录/风险点，按标的裁剪；状态色语义：hit 绿=正面/miss 红=风险/pending 灰=中性。补充修订二收紧：**每块=单个 `.trig` 行**，dot+cond+mt+status 全内联，**禁止 trig 行后另起 `<p>` 正文**——论据压进 `.trig-mt`（一句：数据+判语，多数据点用分号），状态（正面/中性/关注，含 pending 待验证）永不单独成行；v4.9.1 补充修订四 卡片化：渲染层纯 CSS 重排为卡片网格（HTML 结构不变）——标题居中独占行（允许两行）、dot+状态居中徽章行、论据独占行左对齐；桌面 3 列（**v4.9.2 起恰 2/4 张时渲染层自动 2 列取整分排**——4=2+2、2=1+1，写作侧无需处理）、≤1024px 两列、≤768px 单列；trig-mt 内关键事实（金额/时间窗/比例，如「累计套现超 11 亿港元」「业绩预盈公告后十日高位窗口」）用 `<strong>` 包裹（渲染为深色加粗），每块至多 1 处；整块 `<p>` 恰为 2 个=判词段+评分末拍段（超出渲染器告警）；**评分末拍必须写依据链**：「评分：X——基准 X.X，加分项（块名+0.x）、扣分项（块名−0.x，引用块内数据）」，不看正文也能复盘分值由来）；

3.6 含 ROIC vs WACC/再投资/分红可持续，**块首用 metric-row 三卡**（v4.9.1：ROIC 近 3 年均值 / WACC（默认 8%，口径见 scoring.md）/ ROIC÷WACC 覆盖倍数））；

`l2_html` **已废弃**（估值独立成估值分，正文并入 `valuation_html`）；

`l3_html` 含三维（**显示编号 4.1-4.3**：4.1 利润增长/4.2 项目确定性/4.3 催化剂），开头 `<ul>` 列 2-3 条已确认事实（4.3 除外——**4.3 催化剂改用 `.trig-strip` 清单列示**（补充修订四：cond=事件名、mt=时间窗+影响一句、status 三态：hit 绿=已发生 / pending 灰=进行中 / miss 红=已证伪；整块 `<p>` ≤2=判词段+可选评分段——与 13 章触发条件前后呼应：4.3 是预判、13 是跟踪核对）；**4.2 项目确定性可选同款清单**（cond=项目名、mt=产能/投产时间+一句、status：hit 已投产 / pending 在建 / miss 延期；单项目或进度交织的标的仍可整段论证——清单是可选形态不是强制））。**内容地板：`l1_html` 的 dim-block <6 或 `l3_html` 的 dim-block <3 → 拒渲染；单块纯文本 <40 字 → 拒渲染、<80 字 → 告警**（一句话凑数不算内容，每块应有论据+数据+判词）。

3.1 维度块内（判词前）可写注释锚点 `<!--SEGMENTS-->` 挂业务构成图、3.2 维度块内可写 `<!--CHAIN-->` 挂产业链位置图、3.4 维度块内写 `<!--FIN_TREND-->` 挂财务趋势图墙（v4.9）、l3_html 的 4.1 维度块首写 `<!--GROWTH-->` 挂利润增长图（v4.9）（字段契约见 segments/industry_chain/fin_trend/growth_plot 行；锚点缺失图追加章末并告警，字段未填锚点静默清除）

## 片段内 HTML 骨架（模型在 fragment 里照用）

**核心结论四卡骨架（`conclusion_html` 专用，v4.9.1 补充修订二；四卡顺序固定 ①→④，row-major）：**

```html
<div class="concl-grid">
  <div class="concl-card">
    <div class="concl-head">关键优势</div>
    <ul><li>水电成本壁垒不可复制（吨铝成本低 2,100 元）</li><li>报表现金含量足（现金含量 1.8×）</li><li>业绩弹性一落地（26H1 净利 +155%）</li></ul>
    <div class="concl-ref">详见 <a href="#s3">3 公司本质</a></div>
  </div>
  <div class="concl-card">
    <div class="concl-head">关键弱点</div>
    <ul><li>铝价周期波动大（2021 净利 −70%）</li><li>负债率高于同业（53% vs 38%）</li></ul>
    <div class="concl-ref">详见 <a href="#s5">5 风险评估</a>（财务弱点并引 <a href="#s3">3.4 财务健康</a>）</div>
  </div>
  <div class="concl-card">
    <div class="concl-head">当前市场认知</div>
    <ul><li>卖方预期 26 年净利 +30%（东财一致预期 08-28）</li><li>定价隐含限产政策延续</li></ul>
    <div class="concl-ref">详见 <a href="#s8">8 市场预期差</a></div>
  </div>
  <div class="concl-card">
    <div class="concl-head">核心投资逻辑</div>
    <ul><li>论点：成本壁垒+现金含量穿越周期</li><li>证伪：水电优惠取消或铝价跌破 14,500</li><li>路径：先交易电解铝限产政策</li></ul>
    <div class="concl-ref">详见 <a href="#s7">7 估值与安全边际</a> · <a href="#s11">11 仓位与时机决策</a></div>
  </div>
</div>
（上行为云铝股份示例：四卡统一 `<ul>` 短列表——每条 ≤12 字短语 + 至多一个全角括注（数字/日期/来源，
≤15 字，括注不加类）；①卡恰 3 条壁垒短语、②卡 2-3 条不回避的弱点、③卡恰 2 条（卖方假设带来源日期/
定价隐含假设）、④卡恰 3 条固定槽位（论点/可观测证伪条件/路径判断）；展开论述一律留给被引章节）
<div class="warning-card"><strong>情绪与筹码提示：</strong>……（条件触发，显著情绪/筹码风险才加）</div>
```

**四卡红线**：
- 四卡是 `.concl-grid` 内**恰好四张 `.concl-card`**，**四卡统一 `<ul>` 短列表**（补充修订四）：
  每条 ≤12 字短语 + 至多一个全角括注（数字/日期/来源，≤15 字，括注不加类）；
  卡头文字固定（关键优势/关键弱点/当前市场认知/核心投资逻辑），渲染器按纯文本关键词+顺序校验。
- 条数与槽位：①恰 3 条壁垒短语（括注带数字锚）／②2-3 条不回避的弱点（括注带数据）／
  ③恰 2 条（卖方假设带来源日期 + 定价隐含假设）／④恰 3 条固定槽位（论点 / 可观测证伪条件 / 路径判断）。
- 每卡卡尾 `.concl-ref`「详见 X 章」索引（只链无条件章节 s3/s5/s7/s8/s11，
  不链条件章节 s10/s12）；展开省略的论据必须能在被引章节找到，结论章不重复展开。
- **禁止 `.metric-row` 三块评分卡、9 维评分表、三轨判词卡**——评分数字在 Hero 与 section-meta 已呈现，正文只写文字。
- **禁止空泛形容词**；弱点不回避，必须点名对应维度/黄灯。
- 当前市场认知必须引用具体卖方（带来源/日期）或写明"未找到卖方覆盖"，与 8 档位一致。
- 核心投资逻辑的证伪条件必须可观测；路径判断写"未来 1-3 个月交易哪个变量"，不能复读估值结论。

**红灯 hero 卡骨架（`l4_html` 章首固定三卡，v4.9.1 补充修订二；判定口径见 scoring.md 红灯节 a/b/c）：**

```html
<div class="flag-hero">
  <div class="flag-hero-card pass">
    <div class="fh-name">a 财务造假与极高杠杆</div>
    <div class="fh-verdict">通过</div>
    <div class="fh-desc">审计标准无保留；商誉/净资产 3.2%；控股股东无质押；非 ST</div>
  </div>
  <div class="flag-hero-card pass">
    <div class="fh-name">b 立案调查与重大违规</div>
    <div class="fh-verdict">通过</div>
    <div class="fh-desc">近三年无立案调查/重大违规披露（公告检索）</div>
  </div>
  <div class="flag-hero-card pass">
    <div class="fh-name">c 主营不可逆衰退</div>
    <div class="fh-verdict">通过</div>
    <div class="fh-desc">核心产品收入连续增长，无被禁/被主要市场抛弃迹象</div>
  </div>
</div>
```

- 未命中 = `pass`「通过」+ `.fh-desc` 小字写核查路径（逐项覆盖该红灯判定口径：a 卡须含审计意见/商誉/
  质押/亏损 ST 四项核查结果）；命中 = `fail`「不通过」+ `.fh-desc` 写命中事实（与顶层 `red_flag`
  字段一致，1 章 danger-card 由脚本同步注入，两处不冲突）。
- 金融股/困境反转等分型豁免项在 `.fh-desc` 注明豁免依据（如「困境反转框架：连续亏损为前提而非否决项」）。

**损失预演三联卡骨架（`l4_html` 内、黄灯扣分明细表之后，v4.10 起固定；分析步骤的权威口径见 scoring.md「损失预演」节）：**

```html
<p><strong>损失预演（假设两年后这笔投资亏损 30%）：</strong></p>
<div class="pm-grid">
  <div class="pm-card"><div class="pm-head">最可能的亏损故事</div>
    <p>3-5 句叙事：亏损最可能由哪条路径造成——驱动变量 → 财务后果 → 估值后果，带数字。</p></div>
  <div class="pm-card"><div class="pm-head">与红黄灯清单的重合度</div>
    <p>这个故事与清单内风险是同一件事的概率多大；点名对应的红/黄灯项。</p></div>
  <div class="pm-card"><div class="pm-head">清单外新风险</div>
    <p>清单外还剩什么；识别出清单外新风险 → 回到红/黄灯补评，并在本文说明已补。</p></div>
</div>
```

- 三卡固定序、缺一不可；清单外风险确实没有，也要写明「未识别出」及理由——空集也是结论。
- 禁止退回单段 `danger-card` 长文（工行 2026-09-09 报告 300 字单段实证：故事/重合度/清单外
  三件事糊在一个段落里，无视觉分隔）。

**维度块（L1-L4 每个维度一个）：**
```html
<div class="dim-block">
  <div class="dim-header">
    <span class="dim-name">3.1 赛道与宏观</span>
    <span class="dim-weight">7%</span>
    <span class="score-line" style="margin-left:auto;margin-bottom:0;"><span class="badge badge-green">8.0</span></span>
  </div>
  <p>分析正文……</p>
  <p><strong>评分：</strong>8.0——一句理由。</p>
</div>
```
**禁止用灰色小字充当末拍**（v4.8.1：比亚迪复盘报告 3.1/3.2/3.5/4.1-4.3 用小字、3.3/3.6 用正常段落，
混用实证；承载该陋习的 `.verdict` 类 CSS 已于 v4.10.2 从模板删除，即使写了也不再有样式）——
末拍一律是正常段落的「评分：」，分值与本块徽章分一致。

**薄块正例（结构样板，内容为示意）**：正文按「判词 → 论据 → 对比 → 评分」四拍写（v4.8.1：原「收口」改名「评分」），
**四拍各自成段**（判词/论据/评分各一个 `<p>`，对比用表格或独立段——禁止四拍挤进同一个 `<p>`，
渲染器会告警；东方电气 2026-08-31 报告四拍粘段实证），每个维度块 200+ 字，论据必须带具体数字
与出处（巨石/金诚信报告即此结构，DeepSeek 式一句话块属不达标）：

```html
<p><strong>判词：</strong>矿山服务龙头，海外收入占比 72% 是核心阿尔法。</p>
<p><strong>论据：</strong>2025 年报海外毛利 31.2 亿（+24%），单体矿山服务合同平均存续 8.5 年，
续约率 91%（年报 P17）。</p>
<p><strong>对比：</strong>国内同业海外占比均值仅 18%，公司 ROIC 五年均值 18.4%，高同业 8-10pct。</p>
<p><strong>评分：</strong>8.0——护城河在长期服务协议锁定，而非单次勘探能力；铜价下行期续约率是唯一要先盯的先行指标。</p>
```

**徽章色规则**：≥7.0 → `badge-green`；4.0-6.9 → `badge-orange`；<4.0 → `badge-red`。

**层小结（每层末尾）**：`<div class="layer-summary"><strong>L1小结：</strong>……</div>`

**L4 黄灯扣分明细**（v4.10 起**表格为唯一正典**；`.deduction` span 降级为正文行内单条引用的可选形态——块级元素、两条之间不要加 `<br>`，不再用于四类明细全列）：

```html
<div class="table-scroll"><table>
<thead><tr><th>类别</th><th>具体风险</th><th class="num">扣分</th><th>依据</th></tr></thead>
<tbody>
<tr><td>b 行业与政策环境</td><td>……</td><td class="num">0.5</td><td>……（带数据与出处）</td></tr>
</tbody>
</table></div>
<span class="source">数据来源：……（表必须配来源标注，缺了拒渲染）</span>
<p>其余类别（a 交易与股东行为、d 经营与公司治理）已核查无扣分。</p>
```

- **只列 points>0 的类别行**——无扣分类别不入表（工行 2026-09-09 报告 a/d「无/0」空行实证），
  表末一句「其余类别（…）已核查无扣分」兜底，核查信号不丢。
- 类别保持 a→b→c→d 相对顺序（跳号允许，乱序触发渲染器告警，行内/表格两形态都查）；
  表中扣分值须与顶层 `yellow_deductions` 的 points 逐项一致（脚本按后者扣分）。

**盈利质量红旗行**（1D 财务健康，四项检查：现金含量/应收/存货/毛利率；审计意见不在此列，供 L4 红灯 a 判定）：每行用三态 span 包裹行首（✓绿 / ✗红 / △橙）：

```html
<span class="flag-ok">✓ 利润现金含量</span>（最低 1.20，五年均 >1）<br>
<span class="flag-bad">✗ 应收账款周转</span> 恶化（12→16 天，+33%）<br>
<span class="flag-na">△ 毛利率</span> 35.1%（待与同业对照）
```

**提示卡**：`.info-card`（蓝/逻辑说明）`.warning-card`（橙/警示）`.danger-card`（红/致命警告+损失预演）

**变更高亮 `.rev`**（回测模式专属，淡黄底）：只标三类——**评分变化**（旧→新+一句原因）、
**被验证/被证伪的关键假设**、**新增重大变量**。用法：行内 `<span class="rev">2026E净利 930→985亿</span>`，
表格整格 `<td class="rev">`。满屏高亮=没高亮，宁缺毋滥。

**指标卡条**（7 三指标卡、Hero 数据卡同款骨架；**内层类名是 `label`/`value`/`sub`，
不要自创 metric-label/metric-value——渲染器虽会归一，但写对才是自描述**）：

```html
<div class="metric-row">
  <div class="metric-card">
    <div class="label">年化中枢期望收益</div>
    <div class="value up">+21.0%</div>
    <div class="sub">基础中值 472 ÷ 现价 390.4 − 1（12个月）</div>
  </div>
  <div class="metric-card">
    <div class="label">赔率（上行/下行）</div>
    <div class="value">2.32</div>
    <div class="sub">(614−461.6)÷(461.6−396)；&gt;1.5 为良好不对称</div>
  </div>
  <div class="metric-card">
    <div class="label">情景离散度</div>
    <div class="value down">46.8%</div>
    <div class="sub">(乐观中值−悲观中值)÷现价；&gt;90% 高发散降仓一档</div>
  </div>
</div>
```

`value` 着色：正收益/利多 `class="value up"`（绿），负值/利空 `class="value down"`（红），中性不加。

**数据来源标注**：`<span class="source">数据来源：……</span>`（每张数据表下方必须有）

**表格**：`<div class="table-scroll"><table>…</table></div>`。**数据格的对齐类一律不加，交给渲染器**——
渲染器按列内容自动配对表头与数据列的对齐类（数字列右对齐/文字列左对齐/居中列、行头
th 与 rowspan/colspan 均处理），写错或漏写类不影响最终输出。**v4.10 起按列多数决平票判左**
——文本为主的表（如 8 章预期差对照表）整表左对齐，同列不再出现「数字右、文字左」锯齿
（工行 09-11 净息差判断行实证）；tbody 行首格写 `<th>`（如公司名行标题）亦可，样式已与
td 归一（斑马纹/hover/冻结列全覆盖）。
**宽表首列冻结**（≥5 列的数据表，如 9 同业当前指标表与趋势表必用）：`<table class="freeze-first">`——窄屏横向滚动时首列常驻。
**当前指标表最优/最差标注**（v4.9.1，9 同业对比；补充修订二起随「公司=行」改为**按指标列标注**）：
每列按指标方向性各标一格——PE/PB/PS/负债率低为优，ROE/毛利率/净利率/增速高为优；最优格
`<td class="cell-best">`（**v4.9.2 起：红色加粗、无底色**）、最差格 `<td class="cell-worst">`（**黑色加粗、无底色**）
（v4.9.1 曾为染底+同色加粗，与隔行斑马纹叠加显花，v4.9.2 甘李报告实证后取消底色）；目标公司行首格既有蓝色加粗高亮不变
（蓝=本表主角，红/黑=列内极值，语义互补；目标公司格亦可同时是极值格）。
渲染器对齐修正只动 num/center，不吞 cell-best/worst。**v4.10 起整表零标注触发渲染器告警**。
**13 章跟踪与触发合并单表**（v4.9.1）：必须 `<div class="table-scroll"><table class="dash-table freeze-first">`——
dash-table 类使指标/当前值/触发阈值三列防折行、触发后操作/数据来源长文列小字换行，行高不再被长文撑飞。
补充修订四起 ≤768px 自动行转卡片：每行一张卡、六列值各带列名标签（渲染层 td::before 生成），免横向滚动；手写结构不变。

**数字千位符**：金额/户数类数值 ≥1000 必须带千位符（`2,949.2 亿`、`9,536.8 亿`、`188,153 户`）；
年份（2025）、PE/PB 倍数、百分比、股价、EPS、评分**不加**。em_fetch 输出已带千位符，fragment 手写数字照此规则。
边界与兜底：**以「万」为单位携带的数值**（23.34 万户、3.0 亿吨）不加千位符但必须带单位——规则只约束裸数值；
Hero 数值卡里 `mcap` 是纯展示直出（写 10330.7 或 10,330.7 均可，**渲染器已做千位符归一兜底**），
`price` 走 `_num` 解析参与计算，**禁止带逗号**——两类字段待遇不同，不要混。

**数字列不混文字值（MANDATORY）**：`class="num"` 的列里，每个单元格都应是数字或含数字的短值
（如 `-2.1%`、`1,300 亿`）。纯文字值（如「双正增长」）会被脚本判为文字格左对齐，和同列数字
右对齐打架，视觉错乱（中国移动/新华保险已实证）。两种处理二选一：① 文字值改成数字表达
（「双正增长」→「同口径 +X%」）；② 该列整体改为文字列（去掉 `num` 类）。禁止「数字列里混一句文字」。
含箭头的区间值（如 `→ 85+ 元`）也会被特殊处理，与纯数字同列时同样错乱——区间值一律用
`38-55 元` 这种纯数字写法，不要加箭头。

**估值-质量散点图**（9 同业对比）：填顶层 `peers_plot` 字段，脚本生成直角坐标系散点图
（横轴 PE、纵轴 ROE，每家同业一个精确点位，目标公司钢蓝高亮，3×3 分带背景；
可选 `size` 字段按市值缩放点位；目标点与同业必须同口径）。
**坐标/刻度/标签位置全部由脚本计算，模型不要手写 SVG 或估算像素。**
**已填 `peers_plot` 就不要再写 matrix-table**——两图并存冗余，渲染器检测到会直接删除手写九宫格并告警。

**估值-质量矩阵表（兜底）**：仅当 `peers_plot` 无法给出（如同业数据不全）时，
在 `peers_html` 里写 `<table class="matrix-table">` 3×3 表格：行=ROE 高(>15%)/中(8-15%)/低(<8%)，
列=PE 低/中/高（阈值按行业带自定），单元格写「公司名(ROE%/PEx)」、空位填 —；
表头**不用加任何类**，目标公司格用 `class="target"`。

**3年趋势表**（9 同业对比；v4.9.1 补充修订二起**公司=行、指标=列**，与当前指标表方向统一）：方向标注**一律用文字**（升/降/缓升/缓降/大升/大降/平），
**不用单个箭头**（↑↓ 不直观）。幅度约定：|Δ|<0.5pct → 平；0.5-2pct → 缓升/缓降；
2-5pct → 升/降；>5pct → 大升/大降。方向词用 `<span class="up/down">` 着色，
**颜色按指标语义**（ROE/毛利率/净利率：升绿降红；负债率：降绿升红）。

**单元格只写「起→终（方向词）」**——中间年份数字**不进表**（三年数字并排反而看不出趋势，
渲染器检测到连续三年份表头或单元格内"年份:数值"堆叠会告警；东方电气 2026-08-31 实证）：

```html
<!-- ✅ 正确：公司行 × 指标列，格内起→终 + 方向词 -->
<tr><td>科大讯飞</td><td>22.1%→20.0%（<span class="down">缓降</span>）</td><td>70.6%→61.9%（<span class="up">降</span>）</td><td>…</td></tr>
<tr><td>同业甲</td><td>13.1%→19.4%（<span class="up">升</span>）</td><td>60.4%→64.2%（<span class="down">升</span>）</td><td>…</td></tr>

<!-- ❌ 不达标①：指标行公司列（旧方向，与当前指标表不统一） -->
<tr><td>ROE变化</td><td>22.1%→20.0%（缓降）</td><td>13.1%→19.4%（升）</td><td>…</td></tr>
<!-- ❌ 不达标②：三年数字并排，趋势不可读 -->
<tr><td>科大讯飞</td><td>2023: 8.0，2024: 15.2，2025: 30.8</td><td>…</td></tr>
```

**趋势表内容标准**（格式规则之外的列内容清单，神华 2026-08-29 只有 2 项撑不起结论后立规）：
- **必选 6 列（固定列序）**：①归母净利 3 年 ②ROE（加权）3 年 ③销售毛利率 3 年
  ④净利现金含量 3 年（经营现金流净额÷归母净利，标注"计算值"）⑤资产负债率 3 年
  ⑥当年 H1 盈利方向。前 5 列分别支撑同业结论的规模/盈利质量/定价权/利润含金量/杠杆维度，
  正好对齐质量轨打分论据。
- **分型附加列**（不替代必选列）：周期股 +「关键商品价格 3 年」；红利/稳定价值 +「股息率或
  分红率 3 年」；成长股 +「营收增速 3 年」。
- **硬要求**：每一列必须在同业结论框（`.conclusion-box`）中被引用（无引用=删列）；
  缺数的格标「—」并在来源行说明，**禁止编造**。
- **数据路径**：妙想 mx 批量一次查询（多公司×多年×多指标）；注意 mx 会混入 H 股口径
  sheet——**只取 A 股口径，ROE 用加权**。

**估值三情景**（7）：**必填顶层 `valuation` 字段**——脚本自动生成目标价走廊（竖向柱）+
三情景对比表 + 三指标卡条（年化中枢/赔率/离散度），全部数字脚本计算（EPS=profit÷shares、
目标价=profit×PE÷shares、中枢/赔率/离散度公式见 SKILL.md Phase 3），类名写死杜绝幻觉。
`valuation_html` 正文只写：估值方法声明、PE 校准逻辑 info-card、条件性 DCF 表/概率提示——
**不要再手写三情景表和三指标卡**（脚本生成版在前，手写会重复）。
仅当无法给出结构化假设时，才退化为手写表格（scenario-table 类）+ 手写 `scenarios` 区间。

**三情景对比表（兜底手写）**：`<table class="scenario-table">`，指标在行、情景在列，
行序=时间维度/触发条件/净利/EPS/PE/目标价/较现价；**v4.10 起 scenario-table 豁免渲染器的
对齐自动修正**（脚本生成版类名写死、对齐属有意设计），手写兜底必须自带齐全类名
（首列裸 `<td>`、数据列 `class="num"`）。
写成"情景在行"的旧方向会被渲染器自动转置并告警。

## 评分计算规则（脚本执行，模型不要手算）

- **质量层（L1/L3）**：层分 = Σ(维度分×层内权重) ÷ 100；不考虑风险质量分 = L1 层分×L1占比 + L3 层分×L3占比
- **1D 红旗扣分**：`red_deductions` 合计从 `scores["1D"]` 中先行扣减（下限 0），再参与 L1 层分计算
- **质量分（最终）** = 不考虑风险质量分 − 黄灯扣分合计（`yellow_deductions` 的 points 求和），下限 0
- **估值分** = 脚本按 `valuation_inputs` 四件套计算，无手填路径（公式与判读唯一权威见
  `scoring.md` 估值分节；`valuation_score` 字段的处理见顶层字段表）
- **时机分** = Σ(时机维度分×权重) ÷ 100（筹码面 67% + 技术面 33%）
- 各层内权重之和≠100、layer_share 之和≠100 → 脚本报错拒绝渲染
- scores 缺质量层维度 → 脚本报错列出缺失键
- 徽章色脚本自动按分数分配；**三轨判词、决策矩阵落位、调节轨迹、最终仓位结论全部由脚本生成**
  （11 章末三轨判定与仓位结论卡；调节优先级与档位序列见 scoring.md 决策主轴节，报告不展开）

## 拒渲染硬校验清单（内容地板，v4.1 起）

以下任一条不满足即报错退出，不生成报告：

- `valuation_inputs` 缺失或四键（pe_ttm/pe_band/div_yield/risk_free）不全；
- `valuation` 缺失，或三情景缺 pess/base/opt，或某情景缺 profit+pe 区间（或 mcap 市值区间）/ horizon，或口径混用/双填/mcap 区间非法；
- `timing_scores` 缺失或缺维度（筹码面/技术面缺一即拒）；`yellow_deductions` 键缺失（无扣分必须显式填 `[]`）；`thesis_html` 为空；`price` 缺失或无法解析为数字；`date` 不匹配 YYYY-MM-DD；
- 黄灯/红旗 `points` 为负值（负扣分 = 绕过上限）；`valuation.shares` ≤0；利润口径 PE 区间倒挂（高<低）或为负；
- 黄灯扣分单项 >1 或累计 >2（应升红灯）；`red_deductions` 单项 >1；
- `red_flag` 非空但 `position_html` 不含「不建议参与」；
- `thesis_html` 三情景手写价与脚本计算中枢价相对偏差 >2% 且绝对差 >0.1；
- `conclusion_html` 纯文本 <120 字；`position_html` 纯文本 <100 字；
- `l1_html` 的 dim-block <6 个；`l3_html` 的 dim-block <3 个；`peers_html` 不含 `<table>`；
- `l4_html` 缺 `.pm-grid` 损失预演三联卡；黄灯扣分明细表行数 ≠ `yellow_deductions` 条数（多列=零扣分空行该删、少列=扣分项没列全）（v4.10 L4 形态门禁）；
- 任何章节 html 字段内含 `<table>` 但 `class="source"` 出现次数 < 表格数（报错指明字段）。

## 章节编号（模板 1-13，v4.1 起；v4.8.1 起 12/13 对调：先复盘后跟踪）

1 核心结论 ｜ 2 关键利润驱动 ｜ 3 公司本质 ｜ 4 未来预期 ｜ 5 风险评估 ｜ **6 质量分汇总（脚本生成）** ｜
7 估值与安全边际 ｜ 8 市场预期差 ｜ 9 同业横向对比 ｜ 10 周期规律（条件） ｜ 11 仓位与时机决策 ｜
12 回测复盘（条件） ｜ 13 跟踪仪表盘

**正文引用规则**：引用章节一律用上表编号/名称（第 3 章 / 3.4 / 第 5 章风险评估）；
禁止 L1/L3/L4/1D 等框架内部代号出现在正文（它们只是评分框架的层名，只允许出现在
评分表、P0 卡与 fill JSON 字段名里；渲染器检出正文残留会告警）。

## 输出

- 自动命名：`{company}-{code}-{质量分}-{估值分}-{date}.html`，写在 fill JSON 同目录；
  回测模式（填了 `prev`）自动变为 `{company}-{code}-{质量分}-{估值分}-复盘-{date}.html`
- 残留 `{{...}}` 或 `【...】` → 脚本报错退出，报告不生成
- 章节片段为空 → 脚本警告列出（CYCLE_HTML 除外，空=删章节）
