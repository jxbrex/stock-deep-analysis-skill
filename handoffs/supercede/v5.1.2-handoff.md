# v5.1.2 交接 — 全 skill 一致性彻查修复（影石同类 22 项）

> **性质：完成交接——本体已实施并验收；末章为历史 handoff 未动工内容的结转存档。**
> 立项来源：2026-09-21 影石创新矛盾修复（v5.1.1）后用户要求「彻查整个 skill 还有没有
> 同类问题」。三路并行探查（校验 vs 渲染口径 / 双数据源与手写脚本并存 / 契约虚言与
> 量纲缺口）报 40+ 条疑似，主会话逐条复核高风险项后去重为中高危 22 项，
> 用户拍板「中高危全修、一版装完」（原计划三版，讨论后定一版）。

## 三大 bug 类（同源于影石矛盾）

1. **校验比对对象 ≠ 渲染消费终态**：校验在原始数组上数/比，渲染排序、过滤、跳过、
   条件退化——影石「数组第 0 项 vs impact 降序首行」的同型。
2. **同一事实两处数据源无硬同步**：手标布尔 vs 脚本计算、手写 HTML vs 脚本生成、
   自由文本数字 vs 结构化数值——v4.11.3 只堵了三种手写，其余形态裸奔。
3. **契约虚言**：schema 声称「拒渲染/告警/脚本比对」但代码不存在或强度不符；
   自由文本数值无量纲规范。

## 改动清单（22 项，测试 141→154）

### 阶段 1：校验对齐渲染终态（7 项）

- `charts_base.py`：新增 `_gap_dim_ok`（gap 行有效性唯一定义）/ `_period_ratio`
  （完成度分母：一致预期优先、缺则经营目标——与子弹图同源）。
- `charts_misc.py`：`build_gap_plot` 过滤改用 `_gap_dim_ok`；`_gap_notes_html` 重构——
  与图同源过滤（cons≤0 剔除、取前 6、<2 不生成）+ `_renumber_note` 按过滤后行序
  脚本改写圈号前缀（text_dims 续行号；此前剔除行后图①注①错位，读者张冠李戴）。
- `validate.py` `_check_cycle_stages`：条数/本轮判定按有效项（缺 name/period 不落图，
  单独告警）；**current ≠1 → 拒渲染**；字段已填而 cycle_html 缺失 → 告警
  （镜像 pe_history 绑定先例）。
- `validate.py` `_check_dcf`：value/implied_g 非空但 `_num` 不可解析 → 告警
  （此前整卡静默消失）。
- `validate.py` `_check_optional_charts`：segments 占比合计按落图行（name+rev_pct 齐全）
  过滤，剔除行告警；holders 期数按渲染口径（num 可解析+日期去重）。
- `validate.py` `_check_peers_plot_target`：比对落在「roe/pe 可解析的 target 点」集合，
  多 target 告警、全不可解析告警。

### 阶段 2：跨字段数字对账（10 项）

- `validate.py` 新增 `_quote_ref`（quote 落盘 JSON 共用读取入口，降级返回 {}）。
- `_check_hero_cross`（新）：**顶层 pe_ttm vs valuation_inputs.pe_ttm >1% → 拒渲染**
  （metric_label 豁免）；horizon 顶层 vs valuation 不一致告警；mcap vs price×shares
  >5% 告警。`_check_misc_required`：Hero ✓ 字段（pe_ttm/mcap/horizon/sub×3）缺失告警；
  **target_sub_html 含具体百分比/价格 → 告警**（区间被脚本覆盖后手写数字必失配）。
- `_check_gap_plot`：「净利」维度 consensus 对落盘 `consensus_np.np_avg`（>1% 告警，
  年份须一致）；ours 对 valuation 基础情景 profit（>2% 告警，镜像目标价维度口径）。
- `_check_growth_consistency`（新）：np_consensus 对落盘换算增速、base.profit 换算增速
  越出 [np_lo−2, np_hi+2] 告警（基数=fin_trend「归母净利」柱末年值；条件不齐静默跳过，
  亏损期不换算）。
- `_check_l4_form`：行检出放宽（`<tr>/<td>` 带属性不再漏检）+ **逐项 points 不一致 →
  拒渲染**（按类别字母配对、解析不出降级告警）。
- `_check_thesis_price_tags`：**三 span 全缺且含价格形态 → 拒渲染**（裸文本绕行一致性
  硬校验的静默口；部分缺维持告警）。
- `_check_optional_charts` triggers 段：hit/miss 行 target 未写「实际值」告警；
  「阈值｜实际值」可解析时方向与 status 矛盾告警。
- `_check_timing_table_cells` 扩展：小表「筹码面/技术面」得分对 `timing_scores`
  （>0.1 告警）；技术面信号中现价/MA60/MA120 数值与方向词对 quote 落盘 `timing`
  （>1% 或方向矛盾告警——**神华假 MA60 修复闭环**，fill-schema「禁止手估」承诺兑现）。
- `_check_period_track`：verdict 超前/滞后对完成度（`_period_ratio`，时间进度=中报 50%/
  一季 25%/三季 75%）矛盾告警。

### 阶段 3：手写表检测 + 契约补齐（5 项）

- `_check_handwritten_dupes`（新）四条告警：p0_html 手写敏感性表（sensitivity 已填）/
  valuation_html 手写三情景表（scenario-table）与三指标卡（年化中枢/赔率/离散度
  metric-row）/ l1_html 手写多年年表（≥4 年份列，fin_trend 已填）/ gap_html 手写
  逐机构对照表（gap_plot 图会生成时）。
- `_check_red_flag_conclusion`（新）：**红旗 ≥2 项（red_deductions 条数）→
  conclusion_html 首卡首句须含「利润真实性存疑」→ 拒渲染**（schema:61 承诺多年、
  零实现的虚言兑现；与 red_flag→「不建议参与」同强度）。
- `_check_slot_widths`（新）：scenario.trigger >15 字 / drivers.chips 档值 >12 字告警
  （槽位契约表补闸，工行 24 字 trigger 实证可复发）。
- `_check_threshold_coverage`（新）：4.3 压力测试阈值数字在 14 章 triggers/dash_html
  找不到 → 告警（「否则删阈值句」从文档承诺落为启发式执行）。
- `scoring.py` `build_dcf_cards`：每股值单位跟随 fill.currency（默认元、港股港元——
  此前硬编码「元」，港股 Hero 港元/DCF 元同屏打架）。

### 文档与基座

- `render_report.py`：RENDERER_VERSION→v5.1.2。
- `references/fill-schema.md`：17+1 处同步（各新校验逐条注记「v5.1.2 起执行」）；
  **sensitivity 行补 impact 估算纪律**（可复算三要素：变动额/净利基数全表统一/边际假设，
  增量收入不增利禁用历史净利率外推；同一因果链只留一个刻度——影石数据核算实证：
  写了基数的毛利率行对了、没写基数的收入增速行 impact=100 虚高 5 倍）。
- `SKILL.md`：产出前自查清单补 v5.1.2 一致性行；Phase 0 补弹性估算纪律同文。
- `tasks/lessons.md`：新增「『schema 说了不要做』不等于『有人会做』：废止一种形态
  必须同步配检测」。

## 验收记录

- `pytest scripts/ -q`：**154 全绿**（141 + 新增 13 用例：gap 附注重编号 / cycle 拒与
  过滤同源 / Hero 互查 / gap·growth 对账 / L4 逐项 / thesis 裸文本 / triggers 核对 /
  timing 落盘比对 / period 判词对账 / 手写表四禁 / 红旗存疑句 / 槽位长度 / DCF 港元）。
- **热核审计（三路并行子代理，glm-5.3-flash）**：P0×2 + P1×7 + P2 若干，全部修复并复测——
  P0-1 thesis「价格形态」正则过宽（「2023-2025」「3-5 家」年份/数量区间误拒，收紧为
  只认带「元」的数字组）；P0-2 L4 逐项比对崩溃链（points=None/非 dict 裸 traceback，
  scoring 层同步改可定位 ValueError）；P1：gap ours 对账补预测年守卫（历史年实绩误告警）、
  `_period_time_pct` 认全一季/三季/H1 标签（此前对账对非中报期整体失效）、cnp.year 非整数
  不再裸 int、L4 行数分母只计 points>0（0 分合法项误拒）、timing 方向词加现在时守卫
  （「曾跌破、现已收复」不误报）、peers_roe 补 isinstance、`_period_ratio` 亏损期累计
  ≤0 → None（与子弹图同源）；P2：`_renumber_note` 前缀放宽任意标签、text_dims 超 ⑥
  剥圈号防撞号、`_num` U+2212 负号归一（研报复制的「−1」曾解析为 +1）、cycle 全无效项
  降级不硬拒、horizon 缺失告警豁免情景级、4.3 阈值数字加边界、l1 年表收紧 ≥3 行、
  gap docstring 失实句修正、L4 文案改「数值多重集一致」（原「按类别字母配对」为过强声称）。
- **夹具比例修正（热核 P2-1）**：minimal_fill fin_trend 归母净利末年 11 亿 vs base.profit
  100 亿不成比例，growth 换算对账恒定误报 809%——fin_trend 四序列成比例化（末年 93 亿）、
  full_fill triggers 补「阈值｜实际值」（每次校验两条假告警清零）；**golden 七份有意重建，
  diff 逐行核验 = 图墙四序列数值 + triggers 两行文本，其余逐字节不变**。
- **expect_valueerror 加 kw 文案锁**（防「被无关规则误拒」假绿），新用例补充：miss 方向
  矛盾 / cycle 0-current / L4 降级与 0 分母 / thesis 非价格区间 / period 一季标签与负累计 /
  renumber 任意标签前缀。
- 开发期实证两例检查有效性：L4 逐项拒渲染在 points 0.5 vs 0.8 场景命中；timing 方向
  矛盾（现价 < MA120 却写「站上 MA120」）命中。

## 遗留决策（交用户）

- **影石创新 fill 修正重渲**（v5.1.1 结转）：门禁下原 fill 被拒（首行收入增速无同名
  驱动卡）——「毛利率 vs 收入增速谁是第一变量」需分析师裁定后修正重渲，未动。
- **drivers.elastic 自由文本 vs sensitivity.impact 数值对账**（v5.1.1 结转）：本版
  仍未做（自由文本解析脆，可能要求 elastic 结构化或由脚本生成）——v5.2 评估。
  **边界（影石数据核算实证）**：amount/elastic 与 impact 的对账抓得到「两处数字分裂」，
  抓不到「两处一致地错」（影石第二行卡 ±5亿、图 100% 反而自洽——虚高的是金额本身），
  因此估算纪律（v5.1.2 已入契约）是治本、对账只是补充，下版评估时勿以②代①。
- **gap_plot street 照抄 E5 明细零校验**（审计 A4）：架构上无比对对象（E5 明细不落盘），
  需先把 E5 明细纳入落盘才有机械校验可能——与 consensus_np 照抄门禁的强度落差仍在，
  立为后续数据链议题。

---

# 结转章节：历史 handoff 未动工内容存档

> 以下自 v5.1.1-handoff 原文结转（该版全部未动工；本版为一致性修复，未触碰任一议题）。
> **全部为「思考与方案存档，未实施，待用户整体研判」。**
> v5.1.1 的「遗留决策」（影石 fill 修正重渲 / elastic 数字对账）已并入上方本版遗留。

## 结转 A：评分体系改革（原 v5.1-handoff 红队三线，2026-09-18 两轮红队评审立项）

**与本版的关联**：本版指导思想对评分的重新定位（主观横向工具、不追求精确、逻辑自洽
为硬通货）部分回应了 C1「精算浪费」的动机——时机分精算到 0.5 精度在新定位下更无必要。
整体研判时应连同指导思想一并考虑，避免两套评分哲学并存。

- **A1 短板约束**：任一 L1/L3 维度分 <4.5 → 质量分=min(质量分, 6.9)（进不了「好公司」
  行）。落点 scoring.py 后置规则 + validate 硬校验。治「明星维度+财务可疑混进重仓」。
- **A2 高分强制自我证伪**：任一维度分 ≥8 或 ≤3 时，该维 dim-block 判词末尾必含
  「反方证据：…」（≥20 字，须含可观测事实）。validate 按 scores 扫描校验。
- **A3 分数分布台账**：新增 scripts/score_dist.py 读 `_archive/_index.jsonl` 聚合
  各维分数分布，兑现「按存量报告分布校准」承诺（校准闭环从未执行）。
- **B 线 风险层去留**：建议方案=扣分并轨、披露不并轨（黄灯从独立扣分层降级为风险
  排查层：a 类并入 1E、c 类并入 1D 趋势轨、b 类压 1A 定档、d 类拆 1E/1B/3B；白盒
  清单保留为披露物；升红灯替代=命中 ≥4 项且分布 ≥3 维 → 强制红灯复核；质量分公式
  删黄灯项）。**前置依赖：A1 须同版实施**（并轨加剧稀释）。决议状态：三项全待拍板
  （含并入后维度内扣分力度是否等比放大）。
- **C1 时机分精算浪费**：三步=时机不对称化（删「≥6 上浮一档」，只做刹车）/ 信号
  瘦身（获利盘两条留一、删 52 周高点条——价格双重计价，12→8 条）/ 下调一档具体化
  为建仓方式写进 12 章。取舍：时机分退出仓位决策链（降级方案=只做不对称化）。
- **C2 分型循环论证**：成长两型合并（快速成长并入成长型，增速分层进 3A 锚）；
  权重浮动只许业务属性驱动、禁财务数值驱动；分型判据清单化（每型 3-4 个 yes/no
  可观测判据进 fill 结构化字段，validate 校验）。**排序最先动——循环污染评分地基。**
- **C3 阶梯跳变+固定权重**：锚表改分段线性插值（思考按档、计算连续）；四件套权重
  随分型浮动（周期 40/30/25/5，稳定价值 30/15/20/35，成长 45/30/25/0，未盈利
  40/30/30/0，困境反转 40/35/20/5）——成长股股息权重归零消除系统性惩罚；机制二
  「封顶后 7.7<8」算术须按各型重算。
- **三线依赖**：C2 与 B 线同动权重表须同版；C1 与 B 线同改决策链须同版；A1 是全体
  前置。建议顺序：A→C2→B→C1/C3，每步独立可回归。
- **议题 E：盈亏比（原「赔率」）命名与分母失真**（v5.1.0 实报后用户提出，已拍板
  改名、**本轮不动代码**）：
  - **名不正**：公式（基础中值−现价）÷（现价−悲观下限）= 盈利率÷亏损率，数学
    无错，但「赔率」是博彩借词——博彩赔率三要件（固定赌注/二元结果/契约化亏损
    边界）在股票语境一个都不成立，名字诱导读者按「19 倍下注机会」误读。已拍板
    改名「**盈亏比**」（reward/risk 标准译名）；改名后评分锚语义不变（≥2→8.5、
    ∞须硬地板→10 的分档对「上下路长度比」依然成立），v5.2 与下批评分改革同版落地
    （charts_scenario 卡文案/scoring.md 锚表措辞/fill-schema 行/golden 重建）。
  - **深跌失真**：分母=现价−悲观下限，是估值判断而非硬地板。锚纪律钉住悲观下限
    不许随价动 → 现价深跌后分母变薄 → 盈亏比爆炸且对价格超敏感（股价每跌 2%
    翻一倍：45 元时 9.4，43 元时 81，42.7 元时→∞），该区间数字失去决策分辨率；
    跨公司比较时薄/厚分母系统性不可比（恒瑞 18.9 vs 假设厚分母股 2，同为
    「价格贴悲观情景」状态）。且与锚纪律存在联动：锁带结构性把深跌股推进失真区间，
    有奖励深跌的倾向——本义合理（跌了该给高分），失真只在分母趋薄段。
  - **双口径追问（未决）**：图纸口径（对悲观下限，位置度量）vs 压力口径（对净现金
    地板，恒瑞 0.59，信念度量）回答不同问题，差值=收益对判断链的依赖度；但并列
    呈现会引发「估值分用哪口径算」的评分级追问（名义 18.9→8.5 分 vs 压力 0.59→3.5
    分，差 1.25 总分）。候选修法各有利弊：分母改 min(悲观下限,硬参考)/薄分母对数化
    或封顶/双口径进评分取 min——**动评分公式，必须思虑周全**，与 C3 阶梯跳变、
    估值锚纪律联动，走 v5.2 整体研判。本期（用户拍板）只呈现层可选加「分母趋薄
    警示」也一并押后。

## 结转 B：决策边界议题（原 v5.0-handoff 议题 A/B）

- **议题 A：4.0 边界的一票否决效应**：临界带 ±0.3 使质量分 4.0-4.3 实际全落
  「<4 不建议参与」——「质地一般·轻仓」档在边界区失效；每个边界的实质门槛上移 0.3
  （系统性保守倾斜）。讨论方向：①4.0 行增设观察池缓冲档；②临界带只对从上向下跨边界
  生效；③±0.3 按边界差异化；④维持现状但票面写清。**未决议。**
- **议题 B：点值阈值+硬跳档全排查**（机制一的对称作业）：中枢为负拦截器、观察池下调
  直落、上浮冻结交互强度、黄灯 2.0/2.1 质变、乐观税 1.15 点值断崖、估值分判词边界
  与矩阵边界的错位——逐一定哪些值得临界透明化、哪些就该硬。**未动工。**

## 结转 C：偏差测量立项（原 v5.0-handoff「本轮已辩过」区）

`_archive/_index.jsonl` 已攒 fill，待 ≥10 份有兑现结果后做「base 情景 vs 实际兑现」
系统性偏差测量——唯一测量偏差而非假设偏差的路径；顺带实测「两次打分漂移 0.5+」论断
本身。**待样本量。**

## 结转 D：卫生债 Top 3（ponytail 全树审计 2026-09-18，下版顺手修）

1. `delete` em_fetch 门面 24 个死 re-export 名（em_core 侧 21 + em_finance 侧 3，
   逐名 grep 零消费）——v4.10.3 收窄后又长回；
2. `shrink` test_em_fetch 的 `__main__` 手工点名清单改 `sorted(globals())` 自动收集
   （手工清单=新用例忘登记则独立运行静默漏跑，v5.0 的 test_17/18 险漏）；
3. `shrink` test_mcap_mode 61 行自造夹具收编为 `minimal_fill(**over)`（疑似项，
   跑 pytest 即可证伪）。

## 已闭环防误记

- v4.11.3 遗留 B/D/E（色值 token 化 / 字号阶梯收敛 / 改版 contact sheet）已随 v5.0
  同班车完成；F 季K PE 频率统一亦已完成。
- v5.0.1 遗留均为已确认边界：单季双联量级差（口径配对优先）、summary_html 手填质量
  依赖模型自律、港股无单季拆分键（自动降级）；三花归档 fill 覆盖为可选运维动作。
- v5.1.0 本体（锚纪律四件套/指导思想/consensus_np 东财落盘/季K 图注与判词）已验收，
  仅「恒瑞按新规回填重渲 + consensus_np 改填 92.96」两项用户决策悬置（结转待办）。
- v5.1.1 本体（第一变量单源加冕/不同名拒渲染/delta 量纲规范）已验收，影石 fill 修正
  与 elastic 数字对账两项转入本版遗留。
